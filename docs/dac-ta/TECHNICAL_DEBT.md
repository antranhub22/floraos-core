# Nợ kỹ thuật

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 2 | `bom.wrapping` chưa có trường `quantity`, nhưng quy ước đếm yêu cầu đếm số lớp giấy gói | Hợp đồng Vision chỉ được thêm trường; việc thêm thuộc phạm vi M01 | P5 |
| 3 | Không có bộ chọn chi nhánh trên giao diện | Bản đầu phục vụ một cửa hàng; RBAC vẫn đỡ sẵn phạm vi chi nhánh | Khi có tổ chức Chuỗi thật |
| 4 | `LocalBudd` ở Prisma 5, core ở Prisma 7 | Nâng `LocalBudd` giữa lúc M05/M06 đang chạy là xáo trộn không cần thiết | P7 |
| 5 | Hạn mức tính bằng truy vấn tổng hợp trên `usage` mỗi lần tạo job | Đơn giản và đúng; chỉ thành vấn đề khi bảng lớn | Khi một tổ chức vượt vài trăm nghìn dòng usage |
| 6 | `ORG_docx` của `LocalBudd` vẫn mô tả năm bảng sẽ chuyển sang core | Sửa cả 12 tệp giữa lúc M05/M06 đang chạy là xáo trộn; đã dán một trang cảnh báo ở đầu | P7 |
| 7 | Vai hệ thống không có ràng buộc duy nhất ở tầng cơ sở dữ liệu | `@@unique([organization_id, key])` không chặn được trùng khi `organization_id` là NULL — Postgres coi mọi NULL là phân biệt, và Prisma 7 chưa khai được `NULLS NOT DISTINCT`. Hiện chặn bằng khoá tư vấn trong `ensureSystemRoles`, có test khoá | Khi Prisma khai được chỉ mục duy nhất riêng phần, hoặc khi vai hệ thống chuyển thành hằng trong mã ở P2 |
| 8 | Bốn bảng thuộc tenant chưa có khoá ngoại về `organizations` | Lược đồ ở đặc tả 07 mục 3 khai `organization_id` là cột thường; bộ gác và bộ test cách ly đang giữ tính đúng đắn | Cùng đợt với P3, khi số bảng thuộc tenant tăng gấp đôi |
| 9 | Người mở tổ chức nhận vai `dieu_hanh`, không phải `experience_user` | Vai `experience_user` chỉ có `K1` và `K2` nên người vừa đăng ký sẽ không đọc nổi tổ chức của chính mình. Đây là một dòng hằng ở `domain/system-roles.ts` | P10, khi Experience Mode chốt ai nhận vai nào |
| 10 | Chưa giới hạn tần suất trên endpoint đăng nhập (`YC-S5`) | Cần một kho đếm dùng chung mà bản này chưa có | P12 |
| 11 | Vai riêng của tổ chức không bao giờ nhận được một năng lực có trần cứng, kể cả một vai "quản lý khu vực" có chủ đích tương đương Điều hành | Trần cứng (`hardCap`) đặt tên các khoá vai HỆ THỐNG cụ thể; vai tự đặt không khớp bất kỳ trần nào — lựa chọn an toàn theo mặc định, xem `permission-resolver.ts` | Khi có tổ chức Chuỗi thật cần một cấp quyền giữa Điều phối và Điều hành |
| ~~12~~ | **ĐÃ CHỐT 09/10 với anh Tony (AskUserQuestion):** mặc định của công tắc `cho_phep_tu_duyet` là `true` — giữ nguyên như mã hiện tại (`DEFAULT_ALLOW_SELF_APPROVAL = true`, `self-approval-policy.ts`), không đổi mã | Đặc tả 02 mục 2 nêu công tắc nhưng không nói giá trị mặc định; `true` khớp phát biểu "Điều hành là tập cha", nay đã là quyết định chốt, không còn là giả định | — Đã trả |
| 13 | `POST /members/invite` tạo `users` với `password_hash` rỗng nhưng chưa có luồng email hay màn nhận lời mời để người được mời tự đặt mật khẩu | Ngoài phạm vi P2 — P2 chỉ dựng bộ gác quyền, chưa dựng luồng thông báo. Người được mời hiện không đăng nhập được cho tới khi luồng này có | Trước khi tính năng mời thành viên dùng thật, không gắn với một pha cụ thể trong lộ trình P0–P12 |

| ~~14~~ | **ĐÃ CHỐT 09/10 với anh Tony (AskUserQuestion):** giữ `cost_credit` là hằng số trong mã (`src/modules/usage/domain/pricing.ts`) — chưa cần bảng cấu hình, chưa cần giá khác nhau theo tổ chức ở giai đoạn này | Chưa có quyết định kinh doanh về gói giá/mô hình định giá; đổi sang bảng cấu hình lúc này là xây trước khi có nhu cầu thật | Mở lại khi có quyết định kinh doanh về gói giá thật — không phải một ngày cụ thể trong lộ trình P0–P12 |
| 15 | `POST /assets/upload-url` dùng `LocalDiskStorageProvider` — ghi vào đĩa cục bộ của tiến trình web, ký URL bằng lại `SESSION_SECRET` qua HMAC | `.env.example` đã có ô `STORAGE_ENDPOINT`/`STORAGE_BUCKET`/`STORAGE_ACCESS_KEY`/`STORAGE_SECRET_KEY` nhưng chưa có khoá thật lẫn adapter S3/R2 đọc chúng; cổng `StorageProvider` đã tách nên đổi adapter sau này không sửa module (kiến trúc V2 mục 3, đặc tả 05 mục 5) | Trước khi lên production thật — đĩa cục bộ không dùng được qua nhiều tiến trình/máy chủ, và không sống sót qua một lần triển khai lại |
| 16 | `job_events.seq` tính bằng đọc `MAX(seq)` rồi ghi `+1`, không khoá | An toàn trong vận hành bình thường vì một job chỉ có đúng một worker sở hữu sau `claimNext` (đặc tả 07 mục 6.1) — nhưng đây là một giả định, không phải một ràng buộc cơ sở dữ liệu chặn được | Nếu có ngày hai tiến trình cùng ghi log cho một job (ví dụ retry chồng lấn xử lý cũ chưa kịp dừng) |
| 17 | `GET /usage/summary` chưa chia theo chi nhánh cho tổ chức Chuỗi (đặc tả 06 mục 10 đòi, đặc tả 07 mục 7 không có cột `branch_id` trên `usage`) | `generation_jobs` có `branch_id` nhưng `usage` thì không — suy chi nhánh qua `usage.job_id → generation_jobs.branch_id` cần một JOIN mà đặc tả 07 không tính tới khi viết bảng `usage` | Khi có tổ chức Chuỗi thật cần xem mức dùng theo chi nhánh — quyết định thêm cột `branch_id` vào `usage` hay JOIN qua `generation_jobs` |
| 18 | *(gần như đã trả 09/09)* `npm run test:tenant` nay **35/35 xanh thật** trên Postgres, sau ba vòng tìm-sửa lỗi qua log thật của anh Tony (`pg_notify` qua `$queryRaw` ném `P2010`; 2 test sai giả định `credit_balance` mặc định 0, bỏ sót credit chào mừng `TRIAL_CREDIT_BALANCE`; 4 test chưa kiểm đúng đường CREDIT vì workspace mặc định luôn là `EXPERIENCE`) | `npm test` (domain thuần ngoài `tests/tenant/`: `asset-rules`/`job-rules`/`pricing`/`quota`/`storage-key`) vẫn chưa có một lần chạy thật xác nhận trong phiên này — chỉ `tsc --noEmit` | Anh Tony chạy `npm test` một lần, xác nhận xanh — khi đó dòng nợ này đóng hẳn, chuyển xuống mục "Đã trả" |
| 19 | Ngưỡng chạy lượt hai (`NGUONG_CHAY_LUOT_HAI=70`, `NGUONG_CHAY_LUOT_HAI_THANH_PHAN=60`) và ngưỡng đồng thuận đếm kế thừa (`NGUONG_LECH=0.25`) là lựa chọn triển khai của P5, chưa đo trên dữ liệu thật | Đã có bộ ảnh vàng đạt nghiệm thu (8 ảnh, 09/12) để đo — cần chạy đo | Khi có thời gian, đo lại độ chính xác theo ngưỡng và điều chỉnh |
| 20 | P5 mới nối kênh LLM (`OpenAIStructuredProvider`, 2 lượt, đồng thuận trung vị qua `chot()`) — CHƯA nối kênh diện tích cụm và kênh chặn trên vật lý (`chot_danh_sach` đầy đủ ba kênh, `count_engine.py`) vào pipeline thật | Đã có bộ ảnh vàng đạt nghiệm thu (8 ảnh, 09/12) — có thể nối và đo 3 kênh | Nối đủ ba kênh trước khi coi M01 hoàn chỉnh |
| 21 | **ĐÃ CHỐT 09/17 với anh Tony: BẮT BUỘC chọn sản phẩm trước khi duyệt.** `approve-analysis.ts` hiện tự tạo `products` mã `AUTO-<8 ký tự>` khi `product_id` là null — phải đổi thành từ chối (409) và bắt người duyệt chỉ ra sản phẩm | Đặc tả 06/07 không nói rõ hành vi duyệt một phân tích chưa gắn sản phẩm nào; `AUTO-…` là giả định để luồng chạy được. Giả định ấy sinh ra sản phẩm rác không ai đặt tên, rồi Product Master đầy mã không đọc được | **Chưa có mã.** Đổi `approve-analysis.ts`, thêm ca thử khoá 409, và soát các dòng `AUTO-…` đã tạo trong dữ liệu phát triển |
| 22 | **ĐÃ CHỐT 09/17 với anh Tony: CÓ quyền thu hồi duyệt, nhưng phí đã trừ thì KHÔNG hoàn.** Hiện `PATCH /vision/analyses/:id` trả 409 sau khi `APPROVED`, không có đường lùi | Duyệt sai một bó hoa thì hiện không sửa được gì. Tách phí khỏi trạng thái duyệt là đúng bản chất: tiền trả cho LƯỢT CHẠY mô hình, không trả cho phán quyết của người duyệt — thu hồi phán quyết không hoàn lại lượt chạy đã tiêu | **Chưa có mã.** Cần: mã năng lực thu hồi riêng (không dùng ké mã duyệt), endpoint `POST /vision/analyses/:id/unapprove`, cuộn lại phần đã ghi vào Product Master, `audit_logs` cả hai chiều, và một ca thử khoá "thu hồi KHÔNG sinh dòng hoàn credit" |
| 23 | `_catalog_block()` trong `openai_structured.py` là khối trống/placeholder — chưa có bảng vật liệu (materials) riêng theo tổ chức để đưa vào prompt, dù `hidden_materials.json` đã copy nguyên từ v1 | Đặc tả 07 mục 9 không khai bảng materials theo tổ chức; v1 dùng Excel tĩnh dùng chung, core P5 chưa có tương đương theo tenant | Khi có yêu cầu vật liệu riêng theo từng cửa hàng/tổ chức |
| 24 | **ĐÃ TRẢ & ĐÓNG DỨT ĐIỂM (Phán quyết PO 23/09/2026 - D20).** Bộ ảnh vàng 8 ảnh chuẩn gán nhãn người thật của AVI GIFT (`labeled_by` Nguoi-A + `verified_by` Nguoi-B) đạt chuẩn nghiệm thu chính thức và ĐỦ ĐỂ BENCHMARK TẤT CẢ CAPABILITIES. **Chủ sản phẩm LOẠI BỎ TUYỆT ĐỐI yêu cầu mở rộng 100 ảnh vàng.** Nợ kỹ thuật này chính thức được đóng vĩnh viễn. Xem `golden/labels/g001.json`...`g014.json`, `golden/TRANG_THAI_GAN_NHAN.md`. |

| 24a | **Chưa trả.** `OpenAIStructuredProvider` gọi API OpenAI thật trên 8 ảnh vàng còn lại (g003-g014) để đối chiếu kết quả với nhãn người. Đã chạy 4/8 (g001, g002, g010, g011 → `golden/ai-proposals-openai/`). Ảnh g003-g007, g009 nằm trong `golden/images/` (NFD Unicode — cần chuẩn hoá NFC trước khi đọc). Chạy script `scripts/chay-openai-golden.py` và mở rộng danh sách, hoặc viết script riêng đọc đúng tên tệp Việt | Trả khi đủ 8 ảnh đối chiếu → chốt chính xác tuyệt đối cho provider Vision |
| 25 | `product_form` trong nhãn dựng tự động suy từ chữ cái đầu mã sản phẩm (B=bó, G=giỏ, K=lẵng) khi tên thư mục không có từ khoá tường minh; và "kệ" (kệ hoa đứng) tạm xếp chung nhóm "lẵng" vì mục 3 của `BO_ANH_VANG.md` chỉ liệt kê 5 dạng bó/giỏ/hộp/bình/lẵng, không có "kệ" | Suy từ quy ước đặt mã quan sát được (`BHC`, `GHKT`, `KHCB`…), chưa xác nhận với anh Tony đây có đúng là quy ước chính thức không | Anh Tony xác nhận quy ước mã, và xác nhận "kệ" nên tính là "lẵng" hay là một `product_form` riêng — sửa `PREFIX_LETTER_FORM`/`FORM_KEYWORDS` trong script nếu cần |

| 26 | Phạm vi M02 ở P6 chỉ dựng engine giá cốt lõi (`quotePrice`/`checkPriceInvariants`/`checkPriceGuard`) + `pricing_rules` CRUD — KHÔNG dựng luồng "thẻ chào giá" (nhóm năng lực `pricing_card`, C1–C28, đã có sẵn trong `capability-catalog.ts` từ harvest R2 nguyên vẹn: phụ phí, hạng đối tác, bắn đơn cho đối tác, chu kỳ thanh toán…). Cùng lý do, nguồn của `effectiveCostVnd` ("giá vốn hiệu lực", tham số đầu vào của `quotePrice`) chưa được quyết — `gia_von.py` (REUSE, 302 dòng, `FloraOS/python-service/analyzer/`) chưa xếp vào đợt harvest H1–H10 nào, và core chưa có bảng vật liệu theo tổ chức (cùng khoảng trống với nợ #23) | Xác nhận với chủ sản phẩm 09/10 (AskUserQuestion trong phiên P6): "chỉ engine giá cốt lõi", để `pricing_card` cho một pha sau — PRD/Checklist P6 cũng chỉ mô tả đúng ngần này, không nhắc thẻ/bắn đơn | Khi có quyết định xây luồng "thẻ chào giá" — lúc đó mới cần chốt luôn nguồn giá vốn (`gia_von.py` hay nhập tay) |
| 27 | Chi phí lá trang trí và cành trang trí CHƯA được cộng vào `quotePrice` — hợp đồng Vision (`bom.wrapping`/phần lá) không có `quantity` cho hai loại này (điểm lệch #8, `RA_SOAT_THU_HOACH.md`), nên không có số để nhân đơn giá | Chủ sản phẩm chốt 09/10 (AskUserQuestion trong phiên P6): để nợ kỹ thuật — công thức chạy đúng mà không cộng khoản này, thay vì đoán một con số kinh doanh không có căn cứ | Khi có dữ liệu thật hoặc bộ ảnh vàng đạt nghiệm thu (nợ #24) để chốt cách tính |
| 28 | `mucThu.ts` (363 dòng) và `uocPhi.ts` (132 dòng) bị `HARVEST_MANIFEST.md`/`RA_SOAT_THU_HOACH.md` xếp REUSE cho M02 ("Mức thu"/"Ước phí") nhưng đọc mã thật thì không liên quan tới giá sản phẩm: `mucThu.ts` là bảng UAT/nghiệm thu năng lực Điều hành (giá trị/cách thử/đạt khi), `uocPhi.ts` là ước phí lượt phân tích ảnh AI + trần ảnh/ngưỡng chi tiêu — thuộc M01/Usage, đã có `usage/domain/pricing.ts` riêng từ P3. Không tệp nào trong hai tệp này được mang sang ở P6 | Đọc mã trước khi harvest thay vì theo tài liệu (đúng bẫy `AGENTS.md` đã cảnh báo — "đọc mã trước khi xếp hạng, đừng xếp theo trí nhớ") — phát hiện khi soát nguồn cho P6 09/10 | Sửa `HARVEST_MANIFEST.md` mục 3.1 (bỏ `mucThu.ts` khỏi R5) và thêm điểm lệch #10 vào `RA_SOAT_THU_HOACH.md` — đã làm cùng đợt này |
| 29 | `sanTran.ts` (bản gốc, 102 dòng) bị xếp REUSE trong `HARVEST_MANIFEST.md` nhưng KHÔNG thuần — có `import 'server-only'` và gọi thẳng một dịch vụ Excel qua `goiDichVu()`/`docHeThong()`. Chỉ phần quyết định (`quyetDinhChan`, đã nằm trong `chanGia.ts`) là REUSE thật; phần tra số phải viết lại thành `PricingRuleRepository` đọc `pricing_rules` — đã làm ở P6 | Đọc mã trước khi harvest — cùng bẫy với nợ #28 | Đã trả trong P6: `price-guard.ts` chỉ mang `quyetDinhChan`, không mang phần gọi dịch vụ Excel |

| ~~30~~ | **ĐÃ TRẢ 09/10 (P9).** `approveOptimization` (`POST /media/optimizations/:id/approve`, `I2`) là đường DUY NHẤT đặt `assets.approval_state = APPROVED`, ghi cùng `audit_logs` trong một giao dịch. `GET /integration/products/:id/master-image` nay trả 200 sau khi ảnh được duyệt — có ca thử `tests/tenant/media-optimizations.test.ts` gọi endpoint đó TRƯỚC (404) và SAU (200) lượt duyệt trong cùng một ca | Cột thêm ở P7 để dựng đúng endpoint đọc theo đặc tả 08 mục 4 ngay lúc đó, thay vì hoãn cả endpoint tới P9 | — Đã trả |
| 31 | `POST /integration/jobs` chấp nhận mọi giá trị `feature`, không có danh sách trắng theo `client` (`LOCALBUDD` lẽ ra chỉ nên tạo job M05/M06, `SocialFlow` chỉ M04b/M07) | Đặc tả 06 mục 11 và đặc tả 08 không nói tới việc giới hạn `feature` theo loại engine; hạn mức/credit (`enqueueJob`, `YC-U3`) vẫn chặn đúng bất kể `feature` là gì — đây là thiếu chặt chẽ về phạm vi, không phải một lỗ hổng hạn mức | Xác nhận với chủ sản phẩm có cần bảng ánh xạ `client → feature được phép` hay không, trước khi cấp token thật cho `LocalBudd`/`SocialFlow` production |

| 32 | Sàn/Trần theo mã sản phẩm (`Giá chào thấp nhất`/`Giá chào cao nhất` của AVI GIFT) lưu ở `products.attributes.priceGuard`, KHÔNG mở rộng `pricing_rules` sang phạm vi theo-sản-phẩm | Chốt với anh Tony 09/10 (AskUserQuestion, phiên P8): mở `pricing_rules` sang phạm vi sản phẩm là thay đổi kiến trúc M02 nói chung, ngoài phạm vi P8 (chỉ "nạp dữ liệu"). `checkPriceGuard` (`price-guard.ts`, P6) đã có sẵn interface `FloorCeilingLookup` theo mã — chưa có route/use-case nào đọc từ `products.attributes` để dựng `FloorCeilingLookup` đó | Khi có route "tra Sàn/Trần lúc chào giá" dùng thật — quyết định đọc từ `products.attributes` hay chuyển hẳn sang `pricing_rules` theo phạm vi sản phẩm |
| 33 | `excel_parser.py` (v1) dò cột ảnh qua bốn tên `Link ảnh`/`Link Ảnh`/`Ảnh`/`Link_Anh` — không tên nào khớp cột thật của dữ liệu AVI GIFT (`Đường dẫn ảnh`). Xem điểm lệch #12, `RA_SOAT_THU_HOACH.md` | Đọc mã trước khi harvest lật ra sai lệch — cùng bẫy với nợ #28/#29 | Đã trả trong P8: `scripts/nap-avi-gift/doc-excel.py` không mang theo lỗi dò tên cột của bản gốc, đọc đúng `Đường dẫn ảnh` |
| ~~34~~ | **VIẾT MÃ XONG 09/10, chờ chạy thật.** Phần ảnh thật của P8 đã dựng: `scripts/nap-avi-gift/doc-phan-tich.py` (đọc `ket-qua/results.jsonl` + kiểm kê `images/`, chạy thật cho **8 lượt phân tích / 16 ảnh**), `domain/analysis-mapping.ts` (thuần, 8 test), `use-cases/import-analyses.ts`, `scripts/nap-avi-gift-phan-tich.ts`, và `tests/tenant/avi-gift-analyses.test.ts` (8 ca). Con số thật là **8**, không phải "9–14" như ước lượng ban đầu — `results.jsonl` có đúng 8 dòng `status: OK`; năm thư mục ảnh còn lại rỗng. Năm mã nối vào danh mục 1.316 có sẵn, ba mã (`GHTM`, `MM17082026`, `KG-20260831-001`) không có trong danh mục nên tạo `products` mới, đánh dấu `attributes.aviGiftImport.notInPriceCatalog = true` (chốt với anh Tony 09/10) | Nguồn là `results.jsonl` chứ KHÔNG phải `Product_Master.xlsx`: tệp jsonl là đầu ra MÁY của v1 (`schema_version` 10), còn Product_Master.xlsx là bản trình bày suy ra từ nó cho người đọc — nạp bản trình bày rồi gọi nó là `raw` là ghi sai nguồn. Lỗi dữ liệu của v1 không mang sang: cột `Mã sản phẩm` của Product_Master ghi `BHSK0001/BHSK0001`, dính cả tên thư mục | Còn lại: chạy `npm run nap:phan-tich -- --org-id=…` trên Postgres thật, rồi đối chiếu `BAN_GIAO.md` và tích ô "Dữ liệu nhập đủ" |
| 35 | `bootstrapAviGiftOrganization` (P8) sinh mật khẩu tạm ngẫu nhiên cho tài khoản admin AVI GIFT (`antranhub@gmail.com`) — core chưa có luồng "bắt buộc đổi mật khẩu lần đăng nhập đầu" | Ngoài phạm vi P8 (nạp dữ liệu); chưa có đặc tả nào cho luồng đổi mật khẩu bắt buộc ở core | Trước khi tài khoản admin AVI GIFT dùng thật — anh Tony đổi mật khẩu ngay sau lần đăng nhập đầu, thủ công, cho tới khi luồng này có |
| 36 | `FloraOS Vận hành/he_thong.json` (dữ liệu vận hành thật của AVI GIFT, đọc trong lúc khảo sát P8) chứa `openai_api_key` ở dạng CHUỖI THÔ, không mã hoá | Phát hiện ngoài ý muốn lúc đọc cấu trúc thư mục nguồn cho P8 — không phải một phần của lượt nạp (script `doc-excel.py` không đọc tệp này) | **Khẩn** — anh Tony xoay khoá OpenAI này ngay trên dashboard OpenAI, không chờ tới lượt nạp P8 tiếp theo. Không tệp nào trong `scripts/nap-avi-gift/` được đọc hay chép `he_thong.json` |

| 37 | Nạp credit cho một tổ chức chỉ làm được bằng `scripts/nap-credit.ts` chạy tay trên máy có quyền truy cập cơ sở dữ liệu — không có endpoint HTTP nào | Chốt với anh Tony 09/10 (AskUserQuestion, phiên soát P1–P8): mở endpoint đòi một mã năng lực "quản trị nền tảng" mà bộ 114 mã chưa có, VÀ một khái niệm quản trị đứng NGOÀI mọi tổ chức mà đặc tả chưa định nghĩa — cả hai là thay đổi kiến trúc, không phải việc mở khoá vận hành. Script gọi `OrganizationRepository.topUpCredit`, đường duy nhất | Khi có mô hình bán hàng thật (gói credit, thanh toán) — lúc đó mới cần một mặt quản trị nền tảng đúng nghĩa |
| 38 | `bootstrapAviGiftOrganization` dựng tổ chức với `credit_balance = 0` và workspace `PRODUCTION`, nên MỌI `enqueueJob` bị `quotaExceeded("Không đủ credit")` cho tới khi có người chạy `scripts/nap-credit.ts` | Con số credit khởi tạo là quyết định kinh doanh, không phải giá trị kỹ thuật — cấy một hằng số vào mã là đoán thay chủ sản phẩm. `nap-avi-gift-vao-core.ts` in sẵn lệnh nạp kèm `organization_id` ngay sau khi bootstrap để không ai quên | Trước ngày cắt (P11) — anh Tony chốt số credit cấp cho AVI GIFT và chạy lệnh |

Đã trả ngày 09/09 (P1): nợ #1 — `npm run test:tenant` nay xanh thật trên bộ test cách ly, tệp thất bại có chủ đích đã gỡ.

Đã trả ngày 09/09: con số trần cứng (18) · dải mã `E1–E8` · đường dẫn `maChucNang.ts` · hạng thu hoạch `count_engine` và `color_engine` · quyết định D1, D2, D3, D4.

Đã trả ngày 09/09 (P5): sự cố sandbox `Cannot find module '@rollup/rollup-linux-arm64-gnu'` chặn `npx vitest`/`npx tsc` — sửa bằng `npm install @rollup/rollup-linux-arm64-gnu --no-save` (kiến trúc arm64 sai trong `node_modules` đã cài sẵn), cho phép chạy thật `vitest`/`tsc --noEmit` trong sandbox từ nay, không còn phải viết mã rồi để đó chưa xác minh được kiểu.

Đã trả ngày 09/10 (phiên soát P1–P8):

- **CI trên `main` đỏ từ P7** — `.github/workflows/ci.yml` thiếu `INTEGRATION_TOKEN_SECRET` mà
  `src/lib/env.ts` bắt buộc từ P7. Bước `npm run db:seed` ném ở dòng import, TRƯỚC `typecheck`, nên
  cổng bắt buộc `test:tenant` chưa từng chạy trên CI suốt P7 và P8. Đã thêm biến vào workflow.
- **P8 không có ca thử nào chạm cơ sở dữ liệu** — đã thêm `tests/tenant/avi-gift-import.test.ts`
  (9 ca) và một ca còn thiếu cho `GET /integration/brand-profile`.
- **`enqueueJob` trả 500 khi hai request cùng `Idempotency-Key` vào đồng thời** — cửa kiểm trùng lặp
  đọc TRƯỚC giao dịch nên cả hai qua được, người thua vỡ ở chỉ mục duy nhất. Dữ liệu vẫn đúng (giao
  dịch cuộn lại, không trừ credit hai lần) nhưng mã lỗi sai. Đã bắt và trả lại job của người thắng
  (`isDuplicateIdempotencyError`, thuần, có test).
- **`importCatalog` chạy 1.316 lượt `findByCode`** — đổi sang `ProductRepository.listExistingCodes`
  (một lượt `IN (…)` mỗi 500 mã), và chặn luôn trường hợp hai dòng trùng mã trong CÙNG tệp nguồn
  (trước đó sẽ vỡ ở `@@unique([organization_id, code])`).
- **`next.config.ts`** — `experimental.typedRoutes` đã dời lên cấp cao nhất ở Next 16.3.
- **Số liệu nguồn AVI GIFT ghi sai trong `TRANG_THAI.md`** — BOM thật là 5.862 dòng/1.297 mã, không
  phải 5.872/1.300. Ghi thêm: chỉ 27/1.316 SKU có đủ cả Sàn và Trần (nên chỉ 27 dòng nhận
  `attributes.priceGuard`), và 3 SKU không có Giá bán.

Đã trả ngày 09/10 (chạy `test:tenant` lần đầu cho nợ #34):

- **`as never` ở bốn tệp Json của P3 — nay đã trả hết.** Nợ này ghi từ P4
  ("`as never` chỉ còn là nợ của bốn tệp P3", `business-profile-repository.ts`)
  và 09/10 nó gây ra một lỗi thật: `createCompletedHistorical` ghi một object
  vào `generation_jobs.result`, mà cột đó là `String?` chứ không phải Json.
  `as never` nhận MỌI giá trị nên `tsc` im lặng cho qua, `eslint` cũng vậy —
  lỗi chỉ lộ khi chạy trên Postgres thật (8 ca đỏ). Đã đổi hết sang
  `InputJsonValue` ở `usage`/`audit`/`job_events`/`assets`/`generation_jobs`,
  và `null as never` thành `Prisma.DbNull` (NULL của SQL, khác `JsonNull` là
  chuỗi JSON `null` nằm TRONG cột — một phân biệt mà `as never` che mất).
  Không còn `as never` nào trong `src/`.
- **`generation_jobs.result` dùng sai nghĩa.** Cột này là PHÁN QUYẾT nghiệp
  vụ (`APPROVED`/`REJECTED`/`WARNING` của Identity Guard, P9 — xem
  `job-rules.ts`), không phải chỗ chứa số liệu lượt chạy. Lượt nạp lịch sử
  không đi qua cổng nào nên không có phán quyết để ghi: `result = null`, số
  liệu chuyển sang `payload` (cột Json đúng nghĩa).

Đã trả ngày 09/10 (P9 đợt một):

- **Nợ #30** — xem dòng đã gạch ở bảng trên.
- **`OrganizationRepository.refundCredit` không có ai gọi.** D3 chốt từ 09/09
  ("job bị Identity Guard từ chối không tính phí khách; credit hoàn lại")
  nhưng tới trước P9 không có mã nào thực thi — đúng tình trạng `addCredit`
  trước sáng nay. Nay có `refundRejectedJob` + `npm run hoan-credit`.

Nợ mới ghi ngày 09/10 (P9 đợt một):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 39 | Chưa có tăng cường ảnh thật — chỗ `ImageEnhancer` cắm `PassthroughEnhancer`, trả đúng byte đưa vào. Ba cổng `Segmenter`/`BackgroundProcessor`/`Upscaler` khai mà chưa hiện thực. `outputs.ratios` (Smart Reframe) trả object rỗng | Chốt với anh Tony 09/10: làm Identity Guard trước. Passthrough KHÔNG giả vờ là enhancer — tên gọi nói thẳng, `generated_flags` đều `false` đúng sự thật, và nó cho một phép thử có đáp án biết trước (ảnh so với chính nó phải ra `SAFE`) mà không adapter thật nào cho được | Đợt hai của P9 — chọn provider theo Technology Selection Matrix (`YC-N5`/`YC-N6`, cũng là mục còn lại của P5) |
| 40 | Hoàn credit (D3) nhất quán SAU MỘT KHOẢNG, không tức thì: worker ra phán quyết `REJECTED` nhưng không được ghi `usage` (`YC-U4`), nên credit chỉ hoàn ở lần chạy `npm run hoan-credit` kế tiếp | `YC-U4` là luật cứng — hạn mức và credit là việc của core, worker không đụng. Đổi sang hoàn tức thì đòi một kênh worker→core mà D6-1 cấm (chỉ nói chuyện qua bảng) | Khi có yêu cầu vận hành thật về độ trễ hoàn credit — lúc đó cân nhắc một tiến trình core `LISTEN` trên kênh job thay vì cron |
| 41 | Khối bốn điểm của Guard lưu ở `job_events` (`event = "guard"`), không có cột riêng | Đặc tả 07 không khai bảng nào cho M04a, và chỗ CẦN đọc khối này nhất là lúc bị từ chối — đúng lúc không có `assets` nào được tạo để gắn metadata vào | Nếu M04a có bảng riêng ở một pha sau, chuyển sang cột thật và giữ `job_events` làm nhật ký |

Nợ mới ghi ngày 09/10 (đợt đồng bộ ba repo):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 42 | ~~`tests/tenant/integration-sso.test.ts` (7 ca) CHƯA chạy trên Postgres thật~~ **ĐÃ TRẢ 09/10 21:56** — anh Tony chạy `npm run test:tenant` trên Postgres thật: 108/108 xanh trên 13 tệp, gồm cả 7 ca mới | Cùng giới hạn đã gặp ở P5/P8/P9: mã và `tsc`/`eslint`/`npm test` xanh trong sandbox, phần cần DB thật thì anh Tony chạy trên Terminal Mac | Ngay lần chạy `npm run test:tenant` kế tiếp trên máy thật — đây là điều kiện nghiệm thu, không phải việc tuỳ nghi |
| 43 | Nhánh SSO của `/integration/*` xác thực bằng JWT stateless: người bị thu hồi phiên gốc (`sessions.revoked_at`) vẫn gọi được cho tới khi JWT hết hạn (tối đa 15 phút) | Đây là bản chất JWT, đã cân nhắc khi chốt TTL ngắn ở B1. Tư cách thành viên thì kiểm lại mỗi lời gọi (`tenantContextFor`), nên người bị gỡ khỏi tổ chức mất quyền NGAY — chỉ việc thu hồi phiên là có độ trễ | Khi có yêu cầu vận hành thật về thu hồi tức thì — lúc đó thêm một lượt tra `sessions` ở nhánh SSO, đổi lấy một truy vấn mỗi lời gọi |
| 44 | `LocalBudd` không gọi `POST /integration/usage`. Chi phí thật phát sinh ở engine ngoài (LLM sinh trang, ComfyUI) không chảy về `usage` của core — core chỉ ghi credit tại điểm tạo job | Ngoài phạm vi đợt này. Cổng chặn hạn mức (`createJob`) đã đóng, nên tổ chức hết credit không sinh trang được; phần còn thiếu là ĐỘ CHÍNH XÁC của chi phí, không phải cái cổng | Khi có bảng giá thật theo chi phí nhà cung cấp — cùng lúc với nợ #14 |
| 46 | ~~**`npm run test:tenant` xoá sạch cơ sở dữ liệu PHÁT TRIỂN.**~~ **ĐÃ TRẢ 09/10** — `test:tenant` trỏ sang `floraos_test` (`DATABASE_URL_TEST` đè được cho CI), `npm run db:test:setup` dựng database đó, và `tests/helpers/database.ts` TỪ CHỐI chạy khi tên database không kết thúc bằng `_test`, kèm câu lỗi nói thẳng phải chạy lệnh gì. Mô tả gốc: `tests/helpers/database.ts` `TRUNCATE` 22 bảng (gồm `users`/`organizations`/`products`) và dùng CHUNG `DATABASE_URL` với môi trường dev — không có database test riêng | Bộ test cách ly phải bắt đầu từ CSDL rỗng, nếu không thì "không tìm thấy" có thể do dữ liệu sót chứ không do bộ gác. Chuyện đó đúng; cái sai là nó chạy trên đúng database chứa dữ liệu thật của AVI GIFT | Sớm. `AGENTS.md` bắt `test:tenant` xanh trước MỌI merge, nên cứ mỗi lần merge là mất dữ liệu dev — đã mất thật ngày 09/10 lúc 21:56, phải chạy lại `nap:danh-muc` + `nap:phan-tich`. Cách trả: database riêng (`floraos_test`) cho `test:tenant`, cộng một chốt chặn từ chối chạy khi tên database không kết thúc bằng `_test` |
| 45 | `SocialFlow` chưa nối gì với core: không middleware SSO, không client HTTP, `organization_id` vẫn là chuỗi `"default"` chứ không phải UUID tổ chức thật | Nhóm B3 của `UNIFIED_SHELL.md`, chưa nằm trong phạm vi đợt 09/10 (anh Tony chốt phạm vi Đợt 0+1+2) | Đợt 3 của `RA_SOAT_DONG_BO_BA_REPO.md` mục 5 |


Nợ mới ghi ngày 09/11 (nối Dashboard Điều hành `/` với dữ liệu thật — GET
/jobs, GET /products, GET /usage/summary, cookie phiên đọc thật ở
`(app)/layout.tsx`):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 47 | `AdminDashboard` fetch dữ liệu qua `fetch()` trong `useEffect` rồi `setState` — `eslint-config-next/core-web-vitals` (`react-hooks/set-state-in-effect`) coi đây là lỗi, không chỉ cảnh báo. Đã tắt rule tại chỗ cho khối `useEffect` đó | Đây là component đầu tiên trong app gọi API thật phía client — repo chưa có SWR/TanStack Query hay quy ước fetch nào khác để theo. Tắt rule tại một chỗ, không tắt toàn cục | Khi chọn một thư viện fetch/cache phía client (SWR hoặc TanStack Query là hai lựa chọn hợp lý nhất với Next App Router) — lúc đó viết lại theo đúng khuôn của thư viện, không còn `useEffect` gọi thẳng `fetch` |
| 48 | Khối "Hàng chờ duyệt" trên Dashboard hiện "Sắp có", không hiển thị dữ liệu thật | `vision.analyses` và `media.optimizations` chỉ có `POST` (tạo, duyệt) — CHƯA có `GET` liệt kê bản ghi đang chờ duyệt (`result IS NULL` hoặc tương đương). Giả một danh sách rỗng hoặc dùng lại dữ liệu mẫu cũ sẽ sai hơn là nói thẳng chưa có | Viết `GET /vision/analyses` + `GET /media/optimizations` (lọc theo trạng thái chờ duyệt), theo đúng khuôn phân trang con trỏ đã dùng ở `listJobs`/`listProducts` — cần soát lại năng lực (`H1`/`H3`/`I1`/`I2`) trước khi mở endpoint đọc mới |

Nợ mới ghi ngày 09/11 (soát mức sẵn sàng triển khai M01 — Phân tích ảnh sản phẩm):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 49 | Job `CANCELLED` và job `FAILED` KHÔNG được hoàn credit. `refundRejectedJob` chỉ nhận job `COMPLETED` mang phán quyết Guard (`isGuardResult`), đúng phạm vi D3 — nhưng credit trừ tại điểm `enqueueJob` nên người dùng huỷ một lượt đang xếp hàng, hoặc gặp một lượt hỏng vì lỗi kỹ thuật, vẫn mất credit của lượt đó | D3 chốt ngày 09/09 chỉ nói về job bị Identity Guard từ chối; hai trạng thái này chưa có quyết định kinh doanh nào phủ. Không tự cấy một luật hoàn tiền vào mã là đoán thay chủ sản phẩm | Chủ sản phẩm chốt chính sách cho `CANCELLED` (khách chủ động dừng) và `FAILED` (nền tảng hỏng) — hai ca có lý lẽ khác nhau, không nhất thiết cùng một quyết định |
| 50 | Hợp đồng `Schema.json` không có `flower_count`, `bud_count`, `damaged_count`. `BO_ANH_VANG.md` mục 6 khai đúng bốn trường đó trong lược đồ nhãn và mục 9 chấm điểm chính trên `flower_count` — máy và thước đo hiện không chia sẻ trường nào để so | Hợp đồng chỉ được THÊM trường (`YC-N3`), nên thao tác này hợp lệ; nhưng phép suy `flower_count` từ `bom.flowers[].quantity` phải chốt trước khi thêm, vì quy ước đếm 3 tính theo cành còn `dvt_dem` mở nhiều đơn vị | Cùng đợt nghiệm thu bộ ảnh vàng (nợ #24) — không đo được provider nào cho tới khi hai bên có chung một trường |
| 51 | `bom.foliage[].quantity` là trường bắt buộc trong hợp đồng, trong khi `QUY_UOC_DEM.md` quy ước 4 yêu cầu lá và cành trang trí ghi tên và **không đếm số** (`count` để trống) | Hợp đồng thu hoạch nguyên vẹn từ v1 (R1), quy ước đếm chốt sau. Hai tài liệu đang nói hai điều khác nhau về cùng một trường | Cùng đợt với nợ #50 — chốt một trong hai bên rồi sửa bên còn lại, không để cả hai cùng đứng |
| 52 | Không có đường nào đưa `product_analyses.approval_state` về `REJECTED`. Giá trị này có trong enum của lược đồ, có trong kiểu TS, `canApproveAnalysis`/`canEditAnalysis` đều xử lý nó — nhưng không endpoint nào đặt nó. Một kết quả sai không bỏ đi được, nó nằm lại hàng chờ duyệt vĩnh viễn | Đặc tả 06 mục 8 chỉ khai `POST /approve`, không khai `POST /reject`. Thêm một endpoint đổi trạng thái nghiệp vụ là quyết định sản phẩm, không phải chỗ trống kỹ thuật | Chủ sản phẩm xác nhận người soát cần "bỏ" một kết quả hay chỉ cần sửa rồi duyệt — cùng lúc với nợ #22 (thu hồi duyệt) |
| 53 | Chưa có công cụ chấm điểm bộ ảnh vàng. `BO_ANH_VANG.md` mục 9 định nghĩa sáu chỉ số (đếm đúng tuyệt đối, sai số tuyệt đối trung bình, tỉ lệ sai nặng, độ khớp cấu phần, độ khớp màu, chi phí và thời gian) và cổng đổi provider dựa vào chúng; không có mã nào tính | Chỉ số tính trên `flower_count`, mà trường đó chưa có ở phía máy (nợ #50). Viết bộ chấm trước khi chốt phép suy là viết vào chỗ trống | Ngay sau nợ #50 — trước khi có nhãn thật thì bộ chấm không chạy được, nhưng nó phải sẵn sàng đúng lúc nhãn xong |
| 54 | `workers/.venv-worker` trên máy phát triển dựng bằng Python 3.9.6 (CommandLineTools), trong khi `workers/pyproject.toml` khai `requires-python = ">=3.11"` và `[tool.ruff] target-version = "py311"` | Môi trường máy, không phải mã nguồn. Ghi lại vì nó có hệ quả thật: bộ test worker chưa từng chạy được bằng chính venv của repo | Dựng lại venv bằng Python 3.11 trở lên trước khi đưa worker lên máy chủ thật |

Đã trả ngày 09/11 (đợt soát mức sẵn sàng triển khai M01):

- **Nợ #49** — hoàn credit nay phủ ba diện, luật ở `usage/domain/refund-policy.ts`
  (`lyDoHoanCredit`, thuần, 6 ca thử): Guard từ chối (D3, như cũ), job bị huỷ,
  job hỏng vì lỗi kỹ thuật. `refundRejectedJob` đổi tên thành `refundJob` và
  chuyển sang `modules/usage/use-cases/` — nó không còn là việc riêng của
  M04a. `GenerationJobRepository.listRejected` thành `listRefundable`, quét
  mọi `feature` thay vì chỉ `media.optimize`, nên lượt phân tích ảnh hỏng
  cũng được hoàn. `npm run hoan-credit` không đổi cách gọi.
- **Nợ #50** — hợp đồng nay mang ba tổng đếm. Chúng KHÔNG hỏi mô hình mà cộng
  từ `bom` ngay sau khi nhận đáp ứng (`vision/analyzer/dem_tong.py`, thuần,
  9 ca thử), nên không bao giờ có hai giá trị bất đồng trong cùng một bản
  ghi. `Schema.json` thêm `so_nu` và `so_hong` cho mỗi dòng hoa — trước đó
  hợp đồng không có chỗ nào khai nụ, nên quy ước đếm 1 và 6 không thực thi
  được. Đơn vị giữ theo `dvt_dem` của từng loài, đúng quy ước đếm 3.
- **Nợ #51** — không phải mâu thuẫn lược đồ: `bom.foliage[].quantity` vốn đã
  là `["integer","null"]`. Thiếu sót nằm ở `Prompt.md`, nay có mục "Lá và
  cành trang trí không đếm số" bảo mô hình khai `null` ở trường đó.
- **Nợ #52** — `POST /vision/analyses/:id/reject` (`H3`), luật thuần
  `canRejectAnalysis` (chỉ `PENDING`), ghi `audit_logs` action
  `product.reject` trong cùng giao dịch, `ly_do` tuỳ chọn vào phần `after`.
  Không chạm Product Master. Có nút trên cả màn kết quả lẫn hàng chờ duyệt.
- **Nợ #53** — `GET /vision/analyses/export` (`H3`) trả CSV mã hoá UTF-8 kèm
  BOM, một dòng cho mỗi cấu phần, cột `nguon` phân biệt số của máy với số của
  người (`domain/analysis-export.ts`, thuần, 11 ca thử). Đây là tệp đối soát
  vận hành, chưa phải bộ chấm điểm bộ ảnh vàng — bộ đó chờ nhãn thật (nợ #24).

Ba lỗ chặn triển khai đóng cùng đợt, không có dòng nợ riêng vì chúng là lỗi
chứ không phải đánh đổi có chủ đích:

- **`POST /assets` nhận `storage_key` bất kỳ từ client.** Bản ghi mang
  `organization_id` đúng nên mọi bộ lọc tenant cho qua, nhưng đường dẫn trỏ
  được sang `org/<tổ-chức-khác>/…` (đọc ảnh xuyên tổ chức, vỡ luật 1 và 2)
  hoặc ra ngoài kho bằng `..`. Nay `storageKeyMatchesContext` dựng lại đường
  dẫn hợp lệ duy nhất phía máy chủ và so khớp; worker rào lần hai lúc đọc.
- **Job lô hỏng giữa chừng nhân đôi kết quả khi chạy lại.** `product_analyses`
  nay có `@@unique([job_id, asset_id])`; worker bỏ qua ảnh đã xong và ghi
  kèm `ON CONFLICT DO NOTHING`. `conn.rollback()` trong nhánh lỗi đã gỡ — nó
  không làm gì trên kết nối `autocommit` và che mất đúng sự thật đó.
- **Worker thoát hẳn khi gặp sự cố hạ tầng.** `run_worker` nay nối lại theo
  thang lùi 1→60 giây; ghi nhật ký không làm chết tiến trình; `SIGTERM` để
  job đang chạy kết thúc rồi mới thoát.

Siết thêm cùng đợt: lời gọi nhà cung cấp có hạn 120 giây và thử lại hai lần;
đáp ứng bị cắt vì chạm trần token, bị từ chối, hay rỗng đều có câu lỗi riêng
thay vì `JSONDecodeError`; ảnh thu về cạnh dài 1400 px theo `config.json`
(tệp đó trước nay được nạp rồi không ai đọc); `PATCH /vision/analyses/:id`
từ chối bản sửa bỏ sót khoá máy đã trả, vì bản sửa thay NGUYÊN bản gốc nên
thiếu khoá là âm thầm xoá trắng trường tương ứng trong Product Master; lượt
sửa nay ghi `audit_logs` action `product.analysis_edit`; `requirements.txt`
nâng lên `psycopg>=3.2` vì `notifies(timeout=…)` có từ bản đó.

Ghi ngày 09/11 (đợt ba bộ máy phân tích ảnh):

Cổng `VisionAnalyzer` nay có ba hiện thực, chọn ở cấp tổ chức. Cả ba trả cùng
hợp đồng `PhanTichSanPhamHoa`, nên Review, Approve, Product Master, M02 và
M03 không biết bộ nào đã chạy — đúng D5-c (cổng ở mức hợp đồng JSON).

| Khoá | Tên hiển thị | Cách chạy | Trạng thái |
|---|---|---|---|
| `openai_structured` | Đầy đủ | Đo bảng màu tại chỗ → gửi ảnh kèm bảng màu và `Prompt.md` (3.536 token) → một hoặc hai lượt, đồng thuận trung vị | Đã chạy thật |
| `openai_direct` | Gọn | Gửi thẳng ảnh kèm `PromptGon.md` (~600 token), một lượt, không tiền xử lý | Chưa đo trên bộ ảnh vàng |
| `local_cv` | Cục bộ | SAM2 tách → Florence-2 gọi tên → `color_engine` (chưa nối) → lắp ráp. Ảnh không rời hạ tầng | Có hiện thực, CHƯA chạy thử trên trọng số thật (nợ #55) |

Đường chọn: `organizations.settings.bo_may_phan_tich` (cùng khối với
`cho_phep_tu_duyet`, không dựng bảng mới) · `GET /vision/engine` (`H1` — ai
chạy phân tích cũng xem được bộ nào đang chạy) · `PUT /vision/engine` (`H4`,
mã năng lực mới, trần cứng `dieu_hanh`) · màn `/bo-may`.

Bộ máy **chốt vào `payload` của job lúc tạo**, worker không tra lại lúc nhận
việc. Điều hành đổi bộ máy giữa lúc một lô đang xếp hàng thì lô đó vẫn chạy
bằng bộ đã chọn khi bấm nút — nếu không, hai ảnh cùng một lô có thể chạy
bằng hai bộ khác nhau và không ai biết. Mỗi lần đổi ghi `audit_logs` action
`vision.engine.change`.

Danh mục năng lực: 114 → 115 mã, trần cứng 31 → 32. `H4` nằm ở dải mã mới
(F–L) nên bộ 76 mã thu hoạch (A–E) không đổi.

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 55 | `local_cv` CÓ hiện thực `BoTachThucThe`/`Sam2Segmenter` và `BoGoiTen`/`Florence2Labeler`, đăng ký thật trong `registry._dung_local_cv`. **ĐÃ CHẠY THỬ THẬT THÀNH CÔNG 2026-09-11** trên máy thật (Apple Silicon, MPS, SAM2.1 Hiera Small + Florence-2-base) — ảnh `golden/images/g043.jpg`: SAM2 tách 37 thực thể hợp lý, pipeline đầy đủ qua `registry.lay_provider("local_cv").analyze()` ra đúng hình dạng `Schema.json`, `flower_count=18`, `confidence=55` (đúng trần đã định vì chưa có bộ phân loại — nợ #56), checklist tự suy đúng | Hai vướng đã gặp và đã sửa khi chạy thật: (1) `microsoft/Florence-2-base` (mã từ xa `trust_remote_code`) vỡ với `transformers>=4.52.1` nói chung và `5.17.0` cụ thể (`AttributeError: forced_bos_token_id`, rồi `EncoderDecoderCache.layers` ngay trong `generate()` của chính `transformers`) — chuyển sang `florence-community/Florence-2-base` (hỗ trợ Florence-2 NGUYÊN BẢN từ `transformers>=5.15.1`, không cần `trust_remote_code`) và ghim đúng `transformers==5.15.1` trong `requirements-local-cv.txt`; (2) gói `sam2` trên PyPI KHÔNG phải của Meta (bên thứ ba, bản cuối 12/2024) — cài đúng bản chính thức bằng `git clone facebookresearch/sam2` + `pip install -e ".[notebooks]"`. Quan sát MỚI, chưa phải lỗi: SAM2 tách nhiều mặt nạ chồng lấp (cả bó hoa lẫn từng phần con của nó), và `<CAPTION>` mô tả nguyên khung cắt nên vài dòng `bom` gần như lặp ý nhau — cùng gốc với nợ #56 (chưa có bộ phân loại loài để gộp/rút gọn nhãn), ghi thêm ở nợ #60 | **Đã đổi 09/11** — `trang_thai` `local_cv` từ `chua_san_sang` sang `thu_nghiem` (ngang `openai_direct`), chốt qua AskUserQuestion với chủ sản phẩm, đúng quy trình D5-c (không tự quyết một mình). Còn lại: đổi bộ MẶC ĐỊNH của cả ba vẫn chờ đo trên bộ ảnh vàng, không đổi bằng lập luận |
| 56 | `local_cv` không có bộ phân loại loài THỊ GIÁC theo danh mục cửa hàng. **Giảm nhẹ một phần 09/11** (theo yêu cầu chủ sản phẩm): `local_cv_species.doan_ma_loai` so CHỮ nhãn thô của Florence-2 với alias tiếng Anh trong `contracts/species_catalog.json` (86 loài trích từ `FloraOS Vận hành/01_NHAP-LIEU.xlsx` sheet "02 Danh mục loại", 2026-09-11) — khớp rõ MỘT loài thì gán `ma`/tên chuẩn, nhập nhằng hoặc không khớp thì vẫn `ma: null` như cũ. `confidence` VẪN giữ trần 55 bất kể có khớp được mã hay không — chưa có đo lường trên bộ ảnh vàng để biện minh nâng trần (D5-c) | Đây KHÔNG phải bộ phân loại thị giác — chỉ so chữ với đúng nhãn Florence-2 tự nhả ra, không nhìn lại ảnh. Không dùng được cho các trường hợp Florence-2 mô tả sai chủ thể, hoặc loài không có alias tiếng Anh trong danh mục (khoảng 15/86 loài, xem `species_catalog.json`). Sheet "08 Cặp dễ nhầm" (63 cặp, dấu hiệu THỊ GIÁC) chưa dùng tới — chỉ có ích khi có bộ phân loại thị giác thật | Trả HẾT khi có bộ phân loại thị giác thật — cần ảnh có nhãn loài để huấn luyện, tức cùng chỗ nghẽn với bộ ảnh vàng (nợ #24) |
| 57 | `local_cv` khai bảy trong mười ô `checklist` là "Không có" vì chưa phân biệt được vai trò từng loài | `Schema.json` không cho `null` ở các ô này. `confidence` thấp của cả bản ghi là thứ nói ra rằng phần này chưa nên tin | Cùng đợt với nợ #56 |
| 58 | Cả ba bộ máy thu cùng `vision.analyze = 1` credit, dù chi phí thật chênh nhau nhiều lần | Chốt với chủ sản phẩm 09/11: giữ một mức, nền tảng chịu phần chênh. Bảng giá theo bộ máy là quyết định kinh doanh, chưa có mô hình bán hàng thật (cùng lý do nợ #14) | Khi có quyết định kinh doanh về gói giá |
| 59 | `openai_direct` chưa đo trên bộ ảnh vàng, nên chưa có căn cứ nói nó đếm kém hơn hay hơn bộ đầy đủ | `BO_ANH_VANG.md` mục 9: "Không đổi bằng lập luận". Màn `/bo-may` nói thẳng điều này thay vì bày ba lựa chọn trông ngang nhau | Cùng đợt nghiệm thu bộ ảnh vàng (nợ #24) |
| 60 | `local_cv` tách nhiều mặt nạ SAM2 chồng lấp cho cùng một vật thể (cả bó hoa lẫn từng bông con trong đó), và `Florence2Labeler` mô tả nguyên khung đã cắt chứ không riêng vùng mặt nạ — sinh nhiều dòng `bom` gần trùng ý nhau thay vì gộp lại. **Giảm nhẹ một phần 09/11 lần hai** (chủ sản phẩm báo lại vẫn thấy dòng mô tả nhầm nền — cánh tay, cái đĩa, tấm ga, con thuyền — trên ảnh thật `KG-20260904-004-1`): thêm `_loc_chong_lap` ở `local_cv_segmenter.py` (bỏ mặt nạ LỚN chứa trọn mặt nạ NHỎ hơn đáng kể — giữ đơn vị đếm được, bỏ khung gộp) và `_anh_da_ap_mat_na` ở `local_cv_labeler.py` (tô nền phẳng NGOÀI mặt nạ đã nở thêm một dải nhỏ, trước khi đưa vùng cắt cho Florence-2 — giữ chút bối cảnh sát vật thể, cắt phần nền ở xa) | Quan sát thật từ đợt chạy thử 09/11 (`golden/images/g043.jpg`, 37 mặt nạ → 11 dòng `bom.flowers` sau khi gộp theo nhãn nguyên văn, nhiều dòng nội dung chồng lấp) và từ `KG-20260904-004-1` (ảnh thật qua `/tai-anh`, chủ sản phẩm chụp màn hình báo lại): nhiều dòng `bom.flowers` mô tả rõ ràng KHÔNG phải hoa/lá gì cả — "a person holding a white plate with a wreath on it", "a close up of a person's arm with a bandage on it", "a close up of a white sheet on a table/bed", "a close up of a body of water with a boat in the background". `_nhom_theo_nhan` gộp theo NHÃN Y HỆT, không gộp theo ngữ nghĩa — hai lượt giảm nhẹ trên là bước LỌC/CẮT ẢNH ĐẦU VÀO cho Florence-2, không phải bộ phân loại thị giác nên không chặn được caption sai hoàn toàn, chỉ giảm bớt cơ hội mô tả sai. Hai tham số mới (`sam2_nguong_chua_toi_thieu`/`sam2_ty_le_lon_hon_toi_thieu` trong `config.json`) lọc theo KHUNG BAO chứ không phải đúng đa giác mặt nạ — có thể lầm ở cụm hoa dày; `florence2_no_mat_na_ty_le` có RỦI RO đi kèm đã ghi rõ trong docstring `local_cv_labeler.py`: tô nền phẳng có thể làm caption XẤU ĐI với ảnh mà bối cảnh xung quanh thật sự giúp mô hình nhận diện (ví dụ một bó hoa cầm trên tay, mất bàn tay cầm khỏi khung có thể khiến mô hình khó đoán tỷ lệ/góc nhìn hơn) | CHƯA đo trên bộ ảnh vàng (nợ #24) — ba tham số mới đều có mặc định HỢP LÝ (0.85 / 1.5 / 0.15), KHÔNG phải số đã đo, đúng D5-c. Trả HẾT khi có bộ phân loại thị giác thật (cùng chỗ nghẽn với nợ #56) VÀ khi có ảnh thật để đo ba tham số này có làm giảm đúng loại lỗi đã thấy hay không, hay lại cắt mất bối cảnh cần thiết ở ca khác |
| 61 | `VISION_ENGINE_MAC_DINH`/`registry.MAC_DINH` đổi thành `local_cv` (D5-e, 09/11) mà chưa có phép đo trên bộ ảnh vàng — ghi đè có chủ đích, không phải kết quả thắng đo được | Chốt với chủ sản phẩm qua AskUserQuestion, có cảnh báo rõ đây là ghi đè D5-d. Tại thời điểm đổi chưa có worker thật nào đang xử lý job nên chưa job nào lỗi ngay; nhưng worker đầu tiên bật lên phục vụ job thật BẮT BUỘC phải có `torch`, `transformers==5.15.1`, `sam2` cùng hai tệp trọng số SAM2 (`sam2.1_hiera_small.pt`) và Florence-2 (`florence-community/Florence-2-base`) trong đúng môi trường thực thi — thiếu một trong số này thì `registry.lay_provider` ném `NotImplementedError` cho MỌI job không tự chọn bộ máy khác qua `H4` | **Đã đổi 09/12 — `local_cv` → `openai_direct`.** Ma trận 4 ảnh (g001, g002, g010, g011): local_cv (conf 55, đếm 5), openai_structured (conf 85–90, đếm 25–80), openai_direct (conf 90–95, đếm 16–60) — nhanh nhất (1 lượt, ~600 token prompt), rẻ nhất (gpt-4o-mini: 0,15/0,6 USD vào/ra vs gpt-4o: 2,5/10). `MODEL_MAC_DINH` `gpt-4o` → `gpt-4o-mini`. Bug config key `model_truc_tiep` → `model_tien_kiem` đã sửa. Còn đo chi tiết trên bộ ảnh vàng (nợ #24, luật mới: chỉ cần 8 ảnh) để xác nhận chính xác tuyệt đối |

Ghi ngày 09/11 (chủ sản phẩm phản hồi ảnh thật `KG-20260904-004-1` qua `/tai-anh`):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 62 | `_dong_hoa`/`_dong_la` không khớp được danh mục thì `name` hiển thị đổi từ nguyên nhãn thô tiếng Anh sang một hằng số tiếng Việt cố định `TEN_CHUA_XAC_DINH` ("Chưa xác định được tên giống") — MẤT thông tin so với trước với đúng những ca Florence-2 mô tả ĐÚNG một loài hoa có thật nhưng loài đó không có alias tiếng Anh trong danh mục 86 loài (khoảng 15/86, xem `species_catalog.json`) | Chủ sản phẩm yêu cầu kết quả hiển thị tiếng Việt (09/11). Dịch máy một câu tiếng Anh bất kỳ sang tiếng Việt cần thêm một bước suy luận không kiểm chứng được ở đây — bịa một bản dịch nghe hợp lý còn tệ hơn nói thẳng "chưa biết", vì người soát có thể tưởng đó là điều mô hình THỊ GIÁC thật sự thấy chứ không phải máy dịch đoán. Nhãn gốc tiếng Anh KHÔNG mất — `dau_hieu_nhan_dang` giờ LUÔN mang nhãn gốc (trước bản này chỉ giữ khi CÓ khớp danh mục), và `/tai-anh` hiển thị nó thành một dòng phụ mờ "Mô hình mô tả: …" dưới tên chính để người soát còn manh mối sửa tay | Khi có nhu cầu thật: một bộ từ điển Anh–Việt riêng cho từ vựng hoa/lá phổ biến (không chỉ 86 loài trong danh mục cửa hàng) sẽ trả lại phần thông tin đã mất mà không cần dịch máy tự do — nhưng đó là một hạng mục riêng, cần chủ sản phẩm xác nhận có đáng làm không trước, vì độ phủ thật phụ thuộc vào nợ #60 (bớt nhãn nhầm nền) đã giảm được tới đâu |

Ghi ngày 09/11 (phạm vi sản phẩm mở rộng sang bộ tính năng hoàn chỉnh — D8 đến D12):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 63 | **ĐÃ TRẢ 09/12 — bảng `occasions` đặc tả ở tài liệu 07 mục 9**, sáu dòng nạp sẵn `is_seed`, `archived_at` thay xoá cứng. Còn lại đúng một việc khi code P14: chuyển cột `Dịp` dạng chuỗi tự do của 1.316 dòng AVI GIFT trong `products.attributes.catalog` sang mã trong bảng, và chuỗi nào không khớp thì để trống cho người soát chứ không đoán. Nội dung nợ gốc giữ lại dưới đây làm hồ sơ: danh mục dịp sử dụng (20/10, Valentine, 8/3, Ngày của Mẹ, khai trương, hoa cưới, dịp riêng của tổ chức) chưa có bảng lẫn cột. M01b sinh `dip_su_dung`, M05 dựng trang chiến dịch theo dịp, M06 lọc catalog theo dịp, M09 nhắc mua theo dịp — bốn module đọc nó, nên nó là entity của core theo luật cắt | Bốn module đó đều chưa xây. Trong dữ liệu AVI GIFT đã nạp, cột `Dịp` nằm ở `products.attributes.catalog` dạng chuỗi tự do — đủ để không mất dữ liệu, chưa đủ để lọc | P14 — cùng pha với M01b, vì đó là chỗ đầu tiên máy ghi vào trường này |
| 64 | Mức trừ credit cho `creative.compose`, `video.generate`, `content.generate` chưa có. D7 chốt ba bộ máy Vision thu cùng một mức, nhưng một lượt video chênh hai bậc so với một ảnh nên không mở rộng D7 sang được | Chốt bảng giá trước khi có lượt chạy thật nào là đặt một con số kinh doanh không có căn cứ — cùng lý do nợ #14 và #58 | D14, chặn go-live của P16, P17, P18. Trong lúc chờ, hạn mức dùng thử không mở ba chức năng này |
| 65 | Lớp dữ liệu cá nhân của khách hàng cuối chưa có cơ sở pháp lý và chưa có luồng xoá theo yêu cầu của chính khách hàng. Lược đồ đã đỡ sẵn (`customer_consents` chèn-chỉ, đặc tả 07 mục 11) | Dựng lược đồ trước và mở luồng sau là thứ tự đúng: lược đồ không thu thập dữ liệu, còn một màn nhập khách hàng thì có | D13, chặn go-live của M09 và M10, không chặn việc viết mã |
| 66 | Số liệu kế thừa: 45 bài đăng và chỉ số lịch sử của thương hiệu gốc đã gán cho AVI GIFT theo quyết định Đ3-3, nên chúng sẽ tính vào số của AVI GIFT | Chấp nhận khi chốt Đ3-3. `campaign_rollups.is_legacy` là chỗ giữ mốc, và `YC-L5` buộc mọi báo cáo phân biệt trước và sau mốc | P20 — khi M11 dựng báo cáo đầu tiên, xác nhận mốc hiển thị đúng |
| 67 | `campaign_rollups` là bộ nhớ đệm dựng lại được, nhưng chưa có việc dựng lại nào — không script nào nạp lại từ `SocialFlow` khi bảng bị xoá hay lệch | Bảng chưa tồn tại. Ghi ra đây để khi dựng nó thì dựng luôn đường nạp lại, chứ không để nó âm thầm thành nguồn sự thật thứ hai | P20 |

Ghi ngày 09/12 (nền AI — cổng, sổ đăng ký, định tuyến, chấm điểm; D15 đến D19):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 68 | Sổ đăng ký mô hình hiện là một tệp mã (`registry._dung_local_cv`), không phải bảng. Bật tắt một mô hình phải sửa mã và triển khai lại; bốn ô giấy phép không có chỗ để lưu | Ba adapter và một năng lực thì một tệp mã vẫn quản được. Nó hết quản được ở P16, khi loại nhà cung cấp thứ hai xuất hiện ở một repo khác | AI-1 |
| 69 | Lời gọi mô hình không mang mức quyền riêng tư, nên sàn quyền riêng tư chưa được cưỡng chế ở đâu. Hiện chỉ có một sự thật gián tiếp: `local_cv` không gửi ảnh ra ngoài, và màn `/bo-may` nói rõ bộ nào gửi ảnh đi | Với một năng lực và một loại dữ liệu (ảnh sản phẩm) thì lựa chọn ở cấp tổ chức là đủ. Nó hết đủ ở P21, khi dữ liệu cá nhân của khách hàng cuối vào hệ thống | AI-1, và bắt buộc trước P21 |
| 70 | Không có chuỗi dự phòng. `registry.lay_provider` ném `NotImplementedError` cho MỌI job khi môi trường worker thiếu trọng số — mặc định nền tảng hiện là `local_cv` (nợ #61), nên một môi trường worker thiếu `torch` hoặc thiếu tệp trọng số làm mọi lượt phân tích hỏng chứ không rơi về `openai_structured` | Tại thời điểm đổi mặc định chưa có worker thật nào đang chạy. Đây là nợ #61 nhìn từ phía kiến trúc: thứ còn thiếu không phải một bản sửa lỗi mà là một chuỗi dự phòng | AI-2 |
| 71 | **ĐÃ TRẢ PHẦN GHI SỐ 09/17 cho đường phân tích ảnh (Vision), đợt hai 17/09 cho tăng cường + biến thể (M04a/M04b).** Worker Vision, Worker M04a (`AIC-08`) và Worker M04b (`AIC-17`) nay đọc `response.usage` của nhà cung cấp khi có, và ghi `input_tokens`, `output_tokens`, `image_count`, `cost_usd`, `duration_seconds` vào `ai_requests` — bốn cột ấy có trong lược đồ từ AI-1 mà trước đó chưa đường nào điền. `cost_usd` tính bằng bảng giá `gia_token`/`gia_token_anh` ở `contracts/config.json` của từng worker (khớp giá niêm yết của nhà cung cấp, tra lại 17/09/2026). Provider chạy cục bộ (`local_cv`, `StudioEnhancer`, pipeline rembg/backdrop của M04b) để `cost_usd` TRỐNG, không ghi 0: 0 USD không phải 0 đồng. Phát hiện kèm khi đo giá M04a: mô hình mặc định `gpt-image-1` đã bị chính OpenAI đánh dấu deprecated trên trang tài liệu của mô hình — bảng giá vẫn đúng cho tới khi đổi mô hình, đổi mô hình là việc khác chưa làm ở đợt này. **Còn mở:** đường M04c (video) và M07 chưa ghi; chưa có màn tổng hợp "mô hình nào đắt hơn giá trị nó tạo ra" | Hoá đơn cuối tháng của nhà cung cấp gộp cả các bộ máy và mọi năng lực, nên không tách được theo tổ chức, theo bộ máy hay theo ảnh — đó là vì sao ô `usd_moi_anh` trong `golden/so-lieu-*.json` ghi "điền tay từ hoá đơn" và chưa bao giờ được điền | Cắm cùng cách vào worker M04c; màn tổng hợp thuộc AI-2 |
| 72 | Ngưỡng chấp nhận chỉ có cho Identity Guard (0,95 và 0,90, đã chốt với chủ sản phẩm). Các năng lực còn lại chưa có ngưỡng, và `local_cv` đang trần `confidence` ở 55 vì chưa có bộ phân loại | Không có dữ liệu có đáp án thì một ngưỡng là một con số trông hợp lý, và một con số trông hợp lý nằm trong mã lâu hơn bất kỳ giả định nào khác | D20, cùng chỗ nghẽn với bộ ảnh vàng (nợ #24) |
| 73 | Danh mục loài chưa là bảng: `species_catalog.json` 86 loài so **chữ** với nhãn mô hình, không nhìn lại ảnh, và 63 cặp dễ nhầm chưa dùng tới. Chưa có vector, nên không truy hồi được theo nghĩa | Đây là nợ #56 nhìn từ phía tri thức. Chuyển sang bảng mà vẫn so chữ thì không thêm giá trị nào; nó chỉ đáng làm cùng lượt bật `pgvector` và nạp cặp dễ nhầm | AI-3 |
| 74 | Không có sự kiện miền. `job_events` là nhật ký một job, `audit_logs` là nhật ký hành động người — không cái nào cho một module đăng ký nghe việc của module khác, nên chuỗi giá trị hiện nối bằng lời gọi trực tiếp và bằng gợi ý trên giao diện | Với ba module lõi thì nối trực tiếp còn đọc được. Nó hết đọc được khi một lượt duyệt ảnh phải mở đường cho biến thể, video, nội dung và catalog cùng lúc | AI-4 |
| 75 | Giấy phép của thành phần không phải mô hình chưa soát: cấu hình build FFmpeg chưa khoá danh sách thành phần, dù bản cơ bản và các thành phần tuỳ chọn không cùng điều kiện giấy phép | Chưa có đường dựng video nào chạy thật. Soát trước khi có mã dùng nó thì không có gì để soát | Trước dòng mã FFmpeg đầu tiên của P17 |
| 76 | Bốn tệp đã rỗng hoá nhưng chưa xoá khỏi cây mã: `src/lib/variant-compositor.ts`, `scripts/media/process_m04b_variants.py`, route `src/app/api/v1/media/background-removal/route.ts` (nay chỉ trả 409) — cả ba từ P24 — và thêm `src/components/creative/creative-studio.tsx` (rỗng hoá 17/09, xem nợ #80: widget này gọi thẳng proxy SocialFlow, không qua Master Image/Subject Integrity, mồ côi từ khi `app/(app)/creative-studio/page.tsx` thay thế nó) | Môi trường viết mã ghi được tệp nhưng không xoá được tệp, nên rỗng hoá là cách duy nhất chặn được đường chạy cũ trong cùng lượt. Route 409 có lý do giữ lại riêng: bản dựng giao diện cũ còn trỏ vào URL đó và cần một câu trả lời đọc được thay vì 404 | `git rm` bốn tệp ở lượt dọn kế tiếp; giữ route 409 cho tới khi chắc không còn client nào gọi |
| 77 | Sáu bối cảnh của M04b (`M04B_VARIANT_PRESETS`) là hằng trong mã, dùng chung cho mọi tổ chức. `YC-M8` đòi "thư viện nền là cấu hình cấp tổ chức" | Watermark đã theo tổ chức thật (`brand_profiles.logo_asset_id`); thư viện nền cần một bảng mà đặc tả 07 chưa khai. Sáu bối cảnh dùng chung vẫn chạy được cho MVP một ngành hàng | Khi có tổ chức đầu tiên đòi bối cảnh riêng, hoặc khi `brand_profiles` mở rộng thành hồ sơ thương hiệu đầy đủ |
| 78 | **CƠ CHẾ ĐÃ DỰNG 17/09, CHƯA XÁC MINH LƯỢC ĐỒ THẬT.** Mở rộng khung ảnh/banner ngang (`YC-M4`, outpainting, `AIC-13`) nay có cổng `ImageExpander` (`providers/base.py`), hai hiện thực (`PadExpander` — lùi về đóng khung cũ, mặc định; `ReplicateOutpainter` — gọi `bria/expand-image` qua Replicate) và một router (`providers/expansion/router.py`). Áp dụng CHỈ cho biến thể "styled" của `dung_bien_the()` (`variant_worker.py`), và CHỈ khi job truyền `expand_provider` khác mặc định `pad` — không ai yêu cầu thì hành vi giữ nguyên như cũ (đệm màu đặc), đúng bài học nợ #80. `ai_requests` ghi dòng `AIC-13` riêng (`cost_usd` theo bảng `gia_luot_replicate` ở `contracts/config.json`) CHỈ khi provider thật sự chạy, không tính khi đã tự lùi về Pad bên trong. **Còn thiếu:** lược đồ input CHÍNH XÁC của `bria/expand-image` (tên trường, cách gửi ảnh, dạng output) chưa xác minh được — mạng ra ngoài của môi trường soạn mã bị chặn ở cấp tổ chức, chỉ tra được qua mã nguồn một Space bọc ngoài chứ không phải tài liệu gốc của Replicate. `ReplicateOutpainter` tự lùi về `PadExpander` trên MỌI lỗi (thiếu token, lỗi 4xx, mạng, timeout) nên lược đồ sai không làm vỡ job, chỉ mất phần sinh nội dung. `REPLICATE_API_TOKEN` để trống trong `.env`/`.env.example`, chờ token thật | Quyết định 17/09: dựng đủ cơ chế/codebase trước, xác minh lược đồ thật sau khi có token — không chặn việc viết mã vào một việc không kiểm soát được từ môi trường soạn mã (mạng ra ngoài bị chặn). `tests/media_ai/test_expansion_router.py` (13 ca) khoá được đường LÙI VỀ AN TOÀN không cần token thật; không khoá được tính đúng của một lượt gọi Replicate thật — giới hạn đã biết trước, không phải thiếu sót | Khi có `REPLICATE_API_TOKEN` thật: gọi thử `ReplicateOutpainter`, đọc lỗi 4xx (nếu có) để sửa đúng tên trường trong `_goi_predictions`, đối chiếu giá `moi_luot` với trang giá chính thức của Replicate, rồi xoá cảnh báo lược đồ chưa xác minh ở `replicate_outpainter.py`/`config.json` |
| 79 | Cổng Subject Integrity đo trên bản bối cảnh (bản qua nhiều bước xử lý nhất) rồi báo cáo số đo đó cho CẢ lượt, gồm cả bản tách nền và bản đóng dấu | Đo từng bản là ba lần đọc mảng điểm ảnh cho một lượt mà hai bản còn lại là dẫn xuất trực tiếp của bản được đo. Đo bản dễ nhất rồi báo cho cả lượt mới là thứ phải tránh, và mã đang làm ngược lại | Nếu có ngày một biến thể được dựng bằng đường riêng chứ không phải dẫn xuất của bản bối cảnh |
| 80 | **ĐÃ KHAI TỬ 17/09.** `POST /api/v1/proxy/api/m04b/background-removal` vẫn là một đường chạy M04b riêng ở repo `SocialFlow`, chưa rà theo chuẩn của P24. Lượt rà (bốn câu hỏi: auth, tổ chức, credit, ghi `assets`) thấy auth và phạm vi tổ chức đều ổn (đã vá ở một đợt trước, JWT `floraos_sso` xác minh chữ ký thật), NHƯNG route chưa từng gọi `POST /integration/usage` (hàm `ghi_usage()` có sẵn trong `SocialFlow/backend/floraos_core.py` nhưng không nơi nào trong repo gọi tới) và không kiểm năng lực (`capabilities/check`) trước khi chạy — bất kỳ phiên đăng nhập hợp lệ nào của một tổ chức đều chạy được vô hạn lần, không tính hạn mức/chi phí. Quyết định: khai tử thay vì vá, vì M04b thật đã dựng xong trong `floraos-core` từ P24 (`workers/media_ai/jobs/variant_worker.py`, `POST /api/v1/media/variants`, năng lực I4/I5) — không còn lý do giữ một đường chạy song song hẹp hơn (chỉ tách nền, không ghép bối cảnh/watermark/4 tỷ lệ/Subject Integrity) và không đo được chi phí. `SocialFlow/backend/m04b/routes.py`: POST trả `410 Gone`; endpoint tải ảnh cũ `GET /assets/:id/download` GIỮ LẠI cho asset đã tạo trước đó. Proxy whitelist của `floraos-core` (`api/m04b`) không cần đổi — vẫn forward đúng, chỉ nay forward tới 410 thay vì 200 | P24 đóng đường chạy trong `floraos-core`; đường ở `SocialFlow` nằm ngoài repo này và cần một lượt rà riêng với cùng bốn câu hỏi (auth, tổ chức, credit, ghi `assets`) | Đã trả — xem `SocialFlow/TECHNICAL_DEBT.md` mục "M04b khai tử, 2026-09-17" |
| ~~81~~ | **ĐÃ TRẢ 09/17.** `server-only` làm năm suite `tests/tenant/` tắt ở bước NẠP, không ở bước chạy: `assets-m04b`, `integration`, `media-optimizations`, `vision-analyses`, `media-variants`. `index.js` của gói đó ném lỗi ngay khi được nạp và chỉ điều kiện xuất `react-server` mới đổi nó thành mô-đun rỗng; vitest không bật điều kiện đó. Đúng một tệp trong `src/` dùng nó — `storage-provider-factory.ts` của lượt gom kho S3/R2 — và năm suite đọc tới nó qua chuỗi route → use-case → factory. Trả bằng alias `server-only` → `tests/helpers/server-only-stub.ts` trong `vitest.config.ts`; mã sản xuất giữ nguyên `import "server-only"` nên chốt thật vẫn làm việc ở bản dựng | Lỗi hiện ra là *"This module cannot be imported from a Client Component module"* — nghe như lỗi của mã ứng dụng, nên nó nằm im qua nhiều lượt chạy. Cái đắt không phải bốn suite đỏ, mà là **bốn suite đỏ trông giống một suite đỏ**: dòng tổng kết chỉ nói "6 failed" và không ai đọc tới mục "Failed Suites" ở giữa output dài | — Đã trả |
| ~~82~~ | **ĐÃ TRẢ 09/17.** `product-copies.test.ts` không gửi `Idempotency-Key`, trong khi `POST /product-copies/generate` bắt buộc header đó theo `YC-U7` và kiểm nó TRƯỚC bước tra tổ chức — nên cả bốn ca của nhóm `generate` dừng ở 400 thay vì 404 / 422 / 200. Ca "idempotent" nay dùng CÙNG một khoá cho hai lượt gọi, đúng điều nó khoá lại. Bỏ luôn hai dòng `console.log` lạc trong bộ test | Test viết ở P14, header được siết sau đó; 10 ca còn lại của tệp đỏ vì lý do khác (Prisma client cũ, thiếu `product_copies.job_id`) nên bốn ca này lẫn vào một đám đỏ lớn hơn và không ai tách ra | — Đã trả |
| ~~83~~ | **ĐÃ CHỐT 09/17 với anh Tony:** bộ máy Vision mặc định là **`openai_structured`**. `VISION_ENGINE_MAC_DINH` đã lật hai lần (`local_cv` → `openai_direct` → `openai_structured`) mà không lần nào ca thử bắt được, vì `vision-analyses.test.ts` im lặng suốt thời gian nợ #81 còn mở. Ca "lựa chọn của A không ảnh hưởng tổ chức B" còn một khiếm khuyết riêng: nó kỳ vọng B đọc ra CHÍNH giá trị A vừa đặt, nên xanh dù lựa chọn có rò sang B hay không — nay kỳ vọng bộ mặc định, mới phân biệt được hai trường hợp | Một hằng số mặc định đổi được bằng một dòng, và ca thử khoá nó lại thì nằm trong một tệp không nạp nổi. Hai lỗi cộng lại thành một giá trị cấu hình không ai canh | — Đã trả |
| 84 | **QUYẾT ĐỊNH CỦA CHỦ SẢN PHẨM 09/17 — đừng sửa ngược.** Thác dự phòng của bộ máy Vision nay TỤT XUỐNG thang chất lượng: `openai_structured` → `openai_direct` → `local_cv`. Bản trước đi ngược lại ("thác chỉ leo lên": hỏng thì trả kết quả TỐT hơn, không phải rẻ hơn) và có ca thử `test_thac_chi_leo_len` khoá luật đó. Anh Tony chốt đảo hướng: một lượt VẪN ra kết quả, chỉ kém hơn, thay vì hỏng sạch và hoàn credit | Đánh đổi đã cân trước khi chốt: kết quả kém hơn nhưng có, so với không có gì. Chấp nhận được VÌ `local_cv` tự hạ `confidence` xuống 55 (nợ #72) nên kết quả rơi vào hàng chờ người soát chứ không lọt thẳng ra ngoài. **Điều kiện của quyết định:** kết quả kém hơn không được đi im lặng — ba chỗ nói ra điều đó (`product_analyses.provider` ghi bộ ĐÃ CHẠY · `ai_requests.fallback_from` · sự kiện `log` của job mang `fallback_from`, thêm 09/17). Bỏ một trong ba là biến quyết định này thành một lượt hạ chất lượng không ai hay. Sàn quyền riêng tư KHÔNG bị nới: `local_cv` vẫn không có đường nào rơi ra nhà cung cấp ngoài, và ca `test_khong_bo_nao_roi_nguoc_len_thang` chặn cả hướng đó | Không phải nợ để trả — là quyết định đã chốt. Dòng này tồn tại để phiên sau đọc `test_thac_chi_leo_len` trong lịch sử git mà không "sửa lại cho đúng tài liệu cũ" |
| 85 | `local_cv` (SAM2 + Florence-2) cho kết quả xa `openai_structured`, nay là đáy thang dự phòng nên nó CHẠY THẬT khi hai bộ trên hỏng. Cần nâng cấp để sát hơn: bộ phân loại loài thay cho so chữ với nhãn mô hình (nợ #73), và một mức `confidence` đo được thay cho trần cứng 55 (nợ #72) | Chủ sản phẩm 09/17: `openai_structured` là bộ "đúng và sát nhất với thực tế". Đặt `local_cv` làm đáy thang là lựa chọn có ý thức — thà một kết quả kém đi kèm cảnh báo còn hơn không có kết quả. Nhưng "kém" ở đây chưa ai đo bằng số: 8 ảnh vàng mới đối chiếu 4 ảnh với OpenAI thật (nợ #24a) và chưa lượt nào đối chiếu `local_cv` với nhãn người | Đo `local_cv` trên đủ 8 ảnh vàng trước, rồi mới quyết nâng cấp phần nào. Cùng lượt với nợ #24a và #73 |
| 86 | M04b hứa "sinh 20–50 biến thể từ một ảnh gốc" (`BO_TINH_NANG_HIEN_TRANG.md` mục 2.2). Mã hiện sinh tối đa BA biến thể một lượt (tách nền · bối cảnh · đóng dấu) ở MỘT tỷ lệ. Chạy nhiều lượt thì ra nhiều biến thể, nhưng chưa có đường chạy lô: chưa chọn nhiều ảnh cùng lúc, chưa quét hết 6 bối cảnh × 4 tỷ lệ trong một job | Ba biến thể một lượt đã đủ cho việc đăng một bài; con số 20–50 là để có kho ảnh cho cả chiến dịch. Dựng đường chạy lô trước khi biết người bán thật sự dùng bao nhiêu biến thể là xây trước nhu cầu — và mỗi biến thể là một lượt GPU có giá, nên đường chạy lô phải đi cùng bảng giá thật (nợ #64, D14) | Chủ sản phẩm 09/17 chuyển sang làm sau. Trả khi có bảng giá biến thể, hoặc khi một tiệm thật đòi kho ảnh cả chiến dịch |

Ghi ngày 09/17 (rà soát hệ thống Template + Product Master Index, phiên audit độc lập với AI Claude — xem `FLORAOS_TEMPLATE_SYSTEM_SSOT.md` và `product-master-index-repository.ts`):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| ~~87~~ | **ĐÃ CHỐT + SỬA MÃ + XÁC MINH XANH 17/09.** Giá bán lẻ chính thức (`products.attributes.price_vnd`) chưa từng được hệ thống ghi ở bất kỳ đâu; `ProductMasterIndexRepository.mapToMasterIndex` trước đây trả `quotePriceVnd = 0` thay vì bịa số — mọi sản phẩm hiển thị trên Catalog/A6/Order line có giá 0đ. **Chủ sản phẩm chốt 17/09: KHÔNG lưu giá niêm yết tĩnh theo sản phẩm — mọi giá bán thật đi qua `quotePrice()` (M02) gắn với một lượt tư vấn/đơn cụ thể.** Đã đổi kiểu `ProductMasterIndex.pricing.quotePriceVnd` từ `number` sang `number \| null`, `mapToMasterIndex` nay trả `null` thay vì `0` — buộc mọi nơi tiêu thụ phải xử lý tường minh "Liên hệ để báo giá" bằng kiểu dữ liệu, không còn dựa vào quy ước ngầm. Việc đổi kiểu kéo theo 10 lỗi `tsc` ở `chat-assistant` (module này đã tự dựng logic "không hiện 0đ giả" riêng quanh kiểu `number` cũ) — đã sửa bằng cách nới `hasRealPrice`/`formatQuotePriceForChat` thành nhận `number \| null` (vị từ thu hẹp kiểu), nới `SuggestedFlowerCard.priceVnd`, và trích biến cục bộ trước khi gác cổng ở `convert-chat-to-draft-order.ts` để TypeScript thu hẹp đúng | M02 (P6) chỉ dựng engine TÍNH giá theo lượt báo giá, không dựng nơi LƯU giá niêm yết cố định — đúng như quyết định 17/09 xác nhận lại: đây không phải lỗi thiếu sót mà là thiết kế có chủ đích. Khác nợ #26 (nguồn giá VỐN `effectiveCostVnd`, không phải giá NIÊM YẾT) | — Đã trả. Xác minh 17/09 trên máy anh Tony: `npx tsc --noEmit` sạch, `npx vitest run` 14/14 xanh (gồm `flower-consultant-rules.test.ts` 8/8 và `product-master-index.test.ts` 6/6) |
| ~~88~~ | **ĐÃ TRẢ 17/09.** `bom.foliage`/`bom.accessories` trong `ProductMasterIndex` (`products/domain/product-master-index.ts`) từng chỉ giữ `string[]` (tên trần), trong khi hợp đồng Vision (`Schema.json`) và tầng duyệt (`analysis-schema-mapper.ts`) đều bắt đủ số lượng/màu/vai trò/chất liệu/chữ in — `mapToMasterIndex` từng chủ động rút gọn khi build Master Index. Đã đổi `bom.foliage` → `FoliageBomItem[]` (`{name, quantity, unit, color, role}`), `bom.accessories` → `AccessoryBomItem[]` bắt buộc (`{name, material, color, quantity, printedText}`), thêm `bom.wrapping: WrappingLayer[]` (mảng lớp gói đầy đủ, cộng thêm bên cạnh `wrapStyle`/`ribbon` cũ). Rà toàn bộ `src/` xác nhận hai trường cũ chưa từng hiển thị ở bất kỳ UI/template nào ngoài chính hàm build, nên đổi cấu trúc không vỡ màn hình nào | Master Index mới dựng cho phiên bản đầu của các projection (florist ticket, order line); rút gọn về tên là đủ cho MVP in phiếu, nhưng vi phạm nguyên tắc "Atomic Disaggregated Fields" mà chính AGENTS.md/SSOT quy định bắt buộc cho mọi trường BOM khác | — Đã trả. Xác minh 17/09 trên máy anh Tony: `npx tsc --noEmit` sạch, `npx vitest run` 487/685 xanh (198 lỗi còn lại đều là `tests/tenant/*` bị chặn vì DB không phải `_test`, không liên quan tới đợt sửa này) |
| ~~89~~ | **ĐÃ TRẢ 17/09.** `mapFlowerRole()` (`product-master-index-repository.ts`) từng dịch `"Hoa điểm xuyết"` (vai trò của HOA theo hợp đồng Vision) thành `"Lá điểm"` (khái niệm thuộc LÁ, không phải hoa) trong `FlowerBomItem.role`. Đã đổi `FlowerBomItem.role` thành đúng 4 giá trị của hoa (`Chủ đạo/Phụ/Điểm xuyến/Lấp đầy`), bỏ hẳn `"Lá điểm"` — rà toàn bộ `src/` xác nhận không nơi nào khác đọc giá trị `"Lá điểm"` để lọc/hiển thị nên đổi an toàn | `FlowerBomItem.role` hiện chỉ có 4 giá trị không khớp 1-1 với 4 giá trị `bom.flowers[].role` của Vision nên phải ánh xạ tạm; chưa gây sự cố quan sát được vì chưa template nào lọc riêng theo `role = "Lá điểm"` | — Đã trả. Xác minh 17/09: `npx tsc --noEmit` sạch, unit test xanh |
| ~~90~~ | **ĐÃ TRẢ 17/09.** `so_nu` (nụ chưa nở), `so_hong` (cành hỏng), `so_tang_lop` (số tầng cắm) được hợp đồng Vision bắt và tầng duyệt hiển thị, nhưng từng biến mất khi lên `ProductMasterIndex`. Đã thêm `FlowerBomItem.budCount`/`.damagedCount` (từ `so_nu`/`so_hong` từng dòng hoa, optional) và `bom.tierCount` (từ `san_xuat.so_tang_lop`, optional) | Master Index bản đầu ưu tiên đủ trường để in phiếu cắm hoa cơ bản; ba số này phục vụ QC/định giá công thợ, chưa template nào cần tới | — Đã trả về mặt dữ liệu (Master Index nay giữ đủ ba số). **Còn nợ khác:** chưa template/UI nào hiển thị ba trường này — mở khi phiếu thợ cắm hoa hoặc bảng giá công cần dùng tới |
| ~~91~~ | **ĐÃ TRẢ 17/09 (P-Fix-4).** `colorPalette.harmonyTone` và `pricing.pricingRuleRef` là hai trường khai trong `ProductMasterIndex` nhưng không có đường mã nào từng gán giá trị thật — luôn `undefined`. Rà toàn bộ `src/` (kể cả `.test.ts`) xác nhận không nơi nào đọc hai field này — đã xoá khỏi type thay vì xây một luật tính "trông hợp lý" khi chưa có tiêu chí thật. Không đụng `pricingRuleRef` của `orders/domain/order-types.ts` — cùng tên nhưng khác luồng (gắn với một đơn hàng cụ thể qua `quotePrice()`), đang dùng thật | Khai trước cho đủ hình dạng type theo tài liệu thiết kế; đúng nguyên tắc "không bịa" nên để trống thay vì tính ẩu | — Đã trả. Xác minh 17/09 trên máy anh Tony (AskUserQuestion, không suy đoán từ vitest thôi): `npx tsc --noEmit` sạch, `npx vitest run` 487/685 xanh, đúng 198 lỗi cũ không liên quan. Thêm lại khi có luật tính "tông màu hài hoà" thật, hoặc khi Master Index thật sự cần trỏ tới một luật giá áp dụng |
| ~~92~~ | **ĐÃ TRẢ 09/17.** Occasion bị mất một nguồn khi lên Master Index: `mapToMasterIndex` trước đây chỉ đọc `identity.dip_su_dung` (1 giá trị AI đoán, M01), không đọc `attrs.salesData.occasions` mà M01b đã ghi riêng sau khi người duyệt chỉnh tay. Đã thêm hàm thuần `mergeOccasions()` (`product-master-index.ts`, không import Prisma) hợp nhất hai nguồn, kèm `product-master-index.test.ts` khoá luật | Hai luồng ghi occasion vào cùng cột `attributes` được xây ở hai đợt khác nhau (M01 rồi M01b); `mapToMasterIndex` viết trước khi `salesData.occasions` tồn tại nên chưa cập nhật theo | — Đã trả. Xác minh 17/09 trên máy anh Tony: `product-master-index.test.ts` 6/6 xanh, `npx tsc --noEmit` sạch |
| ~~93~~ | **ĐÃ TRẢ 17/09.** *(Đổi số từ #87 trùng — phát hiện lúc rà lại bảng cho nợ #87/#92: hai nợ khác nhau từng cùng mang số 87. Nội dung gốc không đổi.)* Bảng tính chi phí ba bộ máy (`Chi-phi-3-bo-may-phan-tich.xlsx`) đã điền số đo thật từ `python3 scripts/chay-du-bo-may.py --bo day-du,gon,cuc-bo --chay-lai`: `openai_structured` 7.521,9 token vào · 824,4 token ra · 7,71 giây/ảnh · $0,02074/ảnh (đo trên 7/8 ảnh — g011 vượt trần tốc độ token/phút của nhà cung cấp, mã lỗi 429); `openai_direct` 29.456 token vào · 666,8 token ra · 5,8 giây/ảnh · $0,004566/ảnh (8/8 ảnh); `local_cv` không tốn tiền nhà cung cấp, 54,68 giây/ảnh (8/8 ảnh) — số này thay cho mốc đặt tạm 40,75 giây từng dựa trên chỉ 2 ảnh. Sheet Chi phí nay liên kết thẳng tới `usd_moi_anh` đo được thay vì dựng lại bằng token trung bình × đơn giá, để không mất phần chiết khấu token cache | Điền số đoán vào bảng chi phí là lặp đúng cái sai mà P24 vừa gỡ ở giao diện M04b: một con số trông hợp lý nằm trong tệp lâu hơn bất kỳ giả định nào khác. Thà để ô vàng trống và nói rõ lấy số bằng lệnh nào, cho tới khi có số đo thật | — Đã trả. Còn mở riêng: chưa đối chiếu độ chính xác `local_cv` với nhãn người (nợ #85), chưa có giá điện/khấu hao máy để quy giờ máy ra tiền, và giá bán 1 credit (nợ #64, D14) chưa chốt nên cột lãi/lỗ trong bảng chưa có nghĩa |
| 94 | `HinhChieuPhanTich.bom.foliage`/`accessories` (`product-copies/domain/analysis-projection.ts`, dùng bởi `copy-scoring.ts` + `product-copy-adapter.ts` để sinh Thẻ chào/nội dung bán hàng AI) chỉ giữ `ThanhPhan {name, quantity, confidence}` — mất `color`/`role`/`material`/`printed_text` mà hợp đồng Vision cung cấp đầy đủ. Đây là một bản chiếu ĐỘC LẬP với `ProductMasterIndex.bom.foliage`/`accessories` (nợ #88, đã trả 17/09) — cùng vi phạm nguyên tắc "Atomic Disaggregated Fields" nhưng ở tầng sinh câu chữ AI, không phải phiếu cắm hoa. Phát hiện khi rà toàn bộ nơi tiêu thụ `bom.foliage`/`bom.accessories` cho P-Fix-2 | `chamFactual`/`chamBrand`/`buildPrompt` hiện chỉ cần tên + số lượng để đối chiếu dữ kiện và liệt kê vào lời nhắc — chưa nơi nào cần màu/vai trò riêng của lá/phụ kiện để sinh câu chữ bán hàng, nên độ rút gọn này chưa gây mất tính năng quan sát được. Không mở rộng `ThanhPhan` ngoài phạm vi P-Fix-2 (chỉ nhắm `ProductMasterIndex`) để tránh sửa lan khi chưa có nhu cầu thật | Khi cần công thức sinh câu chữ mô tả màu/vai trò riêng từng lá/phụ kiện (vd "viền lá bạc" thay vì chỉ "lá bạc"), hoặc khi có quyết định hợp nhất hai tầng chiếu (`HinhChieuPhanTich` và `ProductMasterIndex`) thành một nguồn duy nhất |

Ghi ngày 17/09 (P-Fix-3 — bổ sung trường thương mại còn thiếu vào Product Master Index, chốt phạm vi với chủ sản phẩm qua hai vòng AskUserQuestion):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| ~~95~~ | **ĐÃ TRẢ 17/09.** Bảng `product_inventory` (tồn kho MẪU sản phẩm dựng sẵn theo chi nhánh, KHÔNG phải nguyên liệu hoa rời — chốt với anh Tony 17/09) đã thêm vào `prisma/schema.prisma`, migration `20260917072209_add_product_inventory` đã chạy thật trên Postgres của anh Tony (chỉ `CREATE TYPE`/`CREATE TABLE product_inventory` — đã soát bằng `grep` trước khi áp dụng, không đụng bảng nào khác), `npx prisma generate` đã chạy. `mapToMasterIndex` nay đọc bảng thật: khớp đúng chi nhánh của SẢN PHẨM (`products.branch_id`, cột tuỳ chọn có sẵn) với dòng `product_inventory` cùng `branch_id` đó; sản phẩm CHƯA gắn chi nhánh cụ thể (hiện là toàn bộ AVI GIFT — nợ #3, chưa có bộ chọn chi nhánh) thì `stock` vẫn `undefined` — gộp tồn kho nhiều chi nhánh thành một trạng thái là quyết định kinh doanh riêng, chưa ai chốt, không suy diễn | Tách migration khỏi phần mã không cần migration để xác minh từng phần, không chặn nhau | — Đã trả. Xác minh 17/09 trên máy anh Tony (AskUserQuestion, không suy đoán từ vitest thôi): `npx tsc --noEmit` sạch, `npx vitest run` 487/685 xanh, đúng 198 lỗi cũ `tests/tenant/*` không liên quan. **Còn mở riêng, không phải phần của nợ này:** quyết định kinh doanh về gộp tồn kho cho sản phẩm chưa gắn chi nhánh — khi có tổ chức Chuỗi thật, cùng nợ #3 |
| 97 | **PHÁT HIỆN + TỰ SỬA 17/09.** Database phát triển `floraos` chưa từng được Prisma Migrate theo dõi trước đợt này — toàn bộ 46 bảng cũ (từ P1 tới P24) được tạo qua `prisma db push` hoặc SQL tay, chưa có `prisma/migrations/` nào trên máy anh Tony. Lần đầu chạy `prisma migrate dev` (cho nợ #95) báo "Drift detected" và đề nghị `prisma migrate reset` — lệnh đó sẽ TRUNCATE toàn bộ dữ liệu AVI GIFT thật. Đã chặn kịp trước khi anh Tony chạy lệnh đó | `db push` là cách nhanh để phát triển sớm (không cần viết migration tay mỗi lần đổi schema) nhưng không để lại lịch sử `_prisma_migrations` — `migrate dev` lần đầu trên một database như vậy luôn coi TOÀN BỘ schema hiện có là "drift" cần đưa database về khớp lịch sử migration (tức là XOÁ SẠCH), vì nó không phân biệt được "trạng thái đã đúng nhưng chưa được ghi sổ" với "trạng thái sai cần sửa". Đây là hành vi mặc định của Prisma, không phải lỗi của mã | — Đã trả 17/09: baseline migration `0_init` dựng từ `git show HEAD:prisma/schema.prisma` (bản schema ĐÚNG như database thật lúc đó, trước khi thêm `product_inventory`), đánh dấu đã áp dụng bằng `prisma migrate resolve --applied 0_init` (chỉ ghi bảng sổ sách `_prisma_migrations`, không đụng dữ liệu) — không dùng `migrate reset`. Từ nay `floraos` đã có lịch sử migration đầy đủ; mọi migration sau này chạy `prisma migrate dev`/`deploy` bình thường, không còn gặp lại "drift" này nữa |
| 96 | `sales-pitch-template.ts` (Thẻ chào A6/M01c) hằng số hoá kích thước mặc định `heightCm = 55`/`widthCm = 40` (dòng 204–205, `overrides?.heightCm ?? 55`) khi nhân viên không nhập tay — một con số kinh doanh bịa ra, không phải đo thật, hệt dạng nợ mà `TECHNICAL_DEBT.md` đã cảnh báo nhiều lần (nợ #14/#58/#93). Phát hiện khi thêm `ProductMasterIndex.dimensions` thật cho P-Fix-3b (trường mới CỐ TÌNH để `undefined` khi chưa có số đo thật, không dùng lại cặp 55×40 này) | Ngoài phạm vi P-Fix-3 (chỉ nhắm `ProductMasterIndex`, không sửa `sales-pitch-template.ts`) — sửa lan sang Thẻ chào A6 khi chưa xác nhận với chủ sản phẩm là đoán thay | Chủ sản phẩm xác nhận: bỏ hẳn mặc định 55×40 (bắt buộc nhập tay hoặc để trống trên Thẻ chào), hay giữ như một ước lượng hiển thị có ghi rõ "ước tính" — cùng lúc có thể nối Thẻ chào đọc `ProductMasterIndex.dimensions` thật thay vì mặc định riêng |

Ghi ngày 17/09 (P-Fix-5 — đồng bộ `FLORAOS_TEMPLATE_SYSTEM_SSOT.md`/`FLORAOS_TEMPLATE_ENGINE_ARCHITECTURE.md`/`AGENTS.md` với code thật, tiếp theo phiên audit P-Fix-0 đến P-Fix-4):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| ~~98~~ | **ĐÃ TRẢ 17/09 (chỉ tài liệu, không đổi mã).** SSOT/Architecture lệch code thật ở nhiều chỗ, rà bằng cách đọc trực tiếp từng tệp thật (không suy từ tên tệp): (1) bảng "Chức năng 6: Danh mục & Báo giá" ghi `templates/catalog/` — thực tế các tệp (`catalog-storefront.tsx`, `share-catalog-modal.tsx`, `product-detail-modal.tsx`, `landing-template-*.tsx`) nằm ở `components/catalog/` (+ `landing-templates/` con); (2) SSOT thiếu 14 tệp thật có trong cây mã: 2 ở `video-studio/` (`video-guidance-card.tsx`, `storyboard-script-card.tsx`) + 1 ở `components/video-studio/` (`video-rendering-progress.tsx`), 4 ở `creative-studio/`, 3 ở `content-engine/`, 7 ở `social-publishing/`; (3) toàn bộ thư mục `platform-connections/` (2 tệp, route `/ket-noi`) không có trong SSOT/Architecture — trở thành "Chức năng 11" chưa từng được đặt tên; (4) `AGENTS.md` dòng 57 ghi đường dẫn `<FeatureGuidanceCard />` sai (`templates/guidance/` — thư mục không tồn tại), thực tế ở `templates/shared/` (xác minh qua stub re-export tại `components/ui/feature-guidance-card.tsx`). Đã sửa cả ba tài liệu, bump SSOT lên "Phiên bản 1.1" | Cả ba tài liệu tích luỹ qua nhiều đợt code mà không có cơ chế đối chiếu tự động (đây chính là lý do P-Fix-6 tồn tại) — không ai chủ đích ghi sai, tài liệu chỉ đơn giản không theo kịp | — Đã trả. Ba tệp đã ghi vào máy anh Tony 17/09 (`device_commit_files`) |
| 99 | Architecture doc (`FLORAOS_TEMPLATE_ENGINE_ARCHITECTURE.md` §3) mô tả bảng Prisma `templates`/`template_overrides` như thể đã tồn tại (sơ đồ tầng 4, cơ chế kế thừa 2 lớp §3.1) — nhưng `prisma/schema.prisma` không có model nào tên này. Toàn bộ cơ chế "tenant override 2 lớp" (System Golden Template → Tenant Override) CHƯA được triển khai; mọi tổ chức hiện luôn rơi về mặc định hệ thống | Sửa tài liệu cho khớp sự thật là an toàn/đảo ngược được; TỰ Ý dựng hai bảng Prisma mới cho một cơ chế chưa ai xác nhận nhu cầu thật là đoán thay chủ sản phẩm — đúng điều AGENTS.md cấm ("không tạo bảng khi chưa rõ phạm vi") | **Đã chốt một phần 17/09 (AskUserQuestion):** anh Tony xác nhận CẦN xây thật (không giữ ở mức định hướng nữa), VÀ nơi lưu là **bảng riêng `template_overrides`** (không dùng `organizations.settings`). **Còn mở:** phạm vi ghi đè được NHỮNG GÌ (chỉ văn bản/thông điệp, hay cả bố cục?) và ghi đè cho HỌ TEMPLATE nào trước (GT/IT/CT/ST/OT) — anh Tony trả lời "chưa biết, cần bàn thêm trước khi viết mã", nên CHƯA viết schema/migration. Ghi chú kỹ thuật để chuẩn bị cho lượt bàn tiếp: template `sales_pitch_zalo` — ví dụ DUY NHẤT Architecture doc §3.1 đặt tên cho cơ chế này — hiện KHÔNG phải dữ liệu (không có trong `core/templates/golden-templates.ts` registry); `generateZaloPitchScript()` (`sales-pitch-template.ts` dòng 277-324) là một chuỗi tiếng Việt HẰNG trong mã (emoji/nhãn cố định), không qua `interpolation-engine.ts` nào cả — nếu chọn family ST làm điểm bắt đầu thì việc đầu tiên không phải "thêm bảng" mà là tách chuỗi hằng này thành golden template trước, rồi mới có gì để ghi đè |
| ~~100~~ | **ĐÃ TRẢ 17/09.** SSOT §6 "Quy trình Kiểm thử" ghi cứng "309/309 test xanh" — con số này không khớp bất kỳ lần chạy thật nào quan sát được trong phiên P-Fix-0 đến P-Fix-4 (`npx vitest run` cho ra 487/685, trong đó 198 lỗi là `tests/tenant/*` bị chặn vì DB không phải `_test`, không liên quan template). Anh Tony chạy đúng lệnh `npm test` (loại trừ `tests/tenant/**`) trên máy thật: **485/485 xanh, 67 tệp test**. Đã thay số thật vào SSOT §6, xoá câu chờ tạm | Bịa một con số mới để thay "309/309" là lặp lại đúng lỗi đang sửa — chỉ khác chỗ sai; nên chờ anh Tony chạy thật thay vì đoán | — Đã trả |
| ~~101~~ | **BÁO ĐỘNG GIẢ — ĐÃ SOÁT LẠI 17/09, KHÔNG PHẢI NỢ THẬT.** Audit ban đầu (trước P-Fix-0) ghi nhầm `templates/product-analysis/sales-pitch-card-a6.tsx` (8.8KB) và `components/sales/sales-pitch-card.tsx` (47KB) là "hai bản trùng chức năng cần hợp nhất". Đọc trực tiếp cả hai tệp (17/09) cho thấy đây KHÔNG phải trùng lặp: `SalesPitchCard` (47KB) là component container/editor đầy đủ (3 tab chỉnh sửa/thẻ/kịch bản Zalo, state cục bộ, xuất PNG/JPEG/PDF/copy clipboard) — và nó **tự `import` rồi render `SalesPitchCardA6`** ở dòng 42 và dòng 1121 để hiển thị đúng phần thẻ A6 bên trong tab "card". `SalesPitchCardA6` (8.8KB) là component TRÌNH BÀY THUẦN (forwardRef, không state, chỉ nhận `activePitch`/`status`/`className`) — đúng vai trò `ST` (Sales Pitch Template) mà SSOT/Architecture mô tả. Đây là kiến trúc lớp đúng (container bọc presentational component), không có gì để hợp nhất hay refactor | Lỗi của audit ban đầu: so kích thước tệp (8.8KB vs 47KB) và tên trùng "Sales Pitch Card" rồi kết luận trùng lặp, không đọc `import` bên trong `components/sales/sales-pitch-card.tsx` để thấy quan hệ cha-con. Đã hỏi anh Tony qua AskUserQuestion dựa trên tiền đề sai này — câu trả lời "giữ bản A6" không cần hành động gì vì không có bản nào bị thay thế, cả hai đều đang dùng đúng vai trò của nó | — Không phải nợ, đóng lại. Không sửa mã, không refactor |

Ghi ngày 17/09 (nghiên cứu sâu nợ #99 — Đề xuất Ghi đè Template theo Tenant, dựng qua Claude Docs, phát hiện thêm khi đọc mã thật `brand_profiles`/M01b/M01c):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| ~~102~~ | **ĐÃ TRẢ 17/09.** `brand_profiles.cta_templates` (Json) từng mang HAI nghĩa xung đột nhau trong cùng một cột: `product-copy-adapter.ts` coi đó là mảng câu CTA (`string[]`), còn `sales-defaults-form.tsx`/`tai-anh/page.tsx` (trước sửa) coi đó là object `{free_gifts, guarantees}` — quà tặng/cam kết mặc định của Thẻ chào A6 và kịch bản Zalo. Hai nghĩa này KHÔNG bao giờ cùng đúng cho một tổ chức: tổ chức nào lưu quà tặng qua `SalesDefaultsForm` thì `cta_templates` không còn là mảng câu CTA nữa, làm hỏng ngầm lời nhắc AI của M01b mà không ai biết. Anh Tony chốt qua AskUserQuestion 17/09: **tách thành hai trường riêng** — `cta_templates` (nay đúng nghĩa MỘT câu CTA, dùng ở M01b) và `default_offers` (`{free_gifts, guarantees}`, dùng ở Thẻ chào A6/Zalo). Đã thêm cột `default_offers Json?` vào `prisma/schema.prisma` (`brand_profiles`), nối qua `brand-profile-repository.ts`/`get-brand-profile.ts`/`put-brand-profile.ts`/`profile-rules.ts`/route `brand-profile`, sửa `sales-defaults-form.tsx` ghi quà tặng/cam kết vào `default_offers` (không còn đè lên `cta_templates`), và nối `ctaPhrase` thật (đọc từ `cta_templates` — nay đúng nghĩa một câu CTA) vào `generateZaloPitchScript()` (`sales-pitch-template.ts`) thay cho câu chốt cứng — có dự phòng đọc hình dạng CŨ (`cta_templates.free_gifts`/`.guarantees`) cho tổ chức đã lưu dữ liệu trước 17/09, không mất dữ liệu AVI GIFT. **Phát hiện thêm khi sửa:** `PUT /brand-profile` thay TOÀN BỘ bản ghi (không patch từng trường — cùng ngữ nghĩa `BusinessProfileRepository`), và `BrandProfileForm` (form màu/giọng văn/hashtag/CTA) không có ô cho `default_offers` nên KHÔNG gửi trường này lên — nếu không sửa, mỗi lần lưu màu/giọng văn ở form đó sẽ XOÁ MẤT quà tặng/cam kết đã lưu ở `SalesDefaultsForm`. Đã sửa `brand-profile-form.tsx` chuyển tiếp nguyên `default_offers` hiện có trong payload, cùng kiểu phòng ngừa mất dữ liệu mà `sales-defaults-form.tsx` đã áp dụng cho `cta_templates` | Cột Json không khoá hình dạng (đặc tả 07 mục 4) nên hai đợt code ở hai thời điểm khác nhau tự do gán hai nghĩa khác nhau cho cùng một cột mà không ai phát hiện — chỉ lộ ra khi rà toàn bộ nơi đọc/ghi `cta_templates` cho nợ #99 | — Đã trả. Xác minh 17/09 trên máy anh Tony: cột `default_offers` đã có thật trong Postgres (xác nhận qua `npx prisma db pull --print`, không suy đoán), `npx prisma generate` xong, `npx tsc --noEmit` sạch, `npm test` **493/493 xanh, 67 tệp test** |
| ~~103~~ | **ĐÃ TRẢ 17/09.** Lỗi crash runtime thật, phát hiện khi sửa nợ #102. `generate-product-copy.ts` (M01b) từng ép kiểu thẳng `brandProfile.hashtags`/`.cta_templates` (Json) thành `string[]` bằng `as string[]` rồi truyền cho `product-copy-adapter.ts`, nơi `buildPrompt()` gọi `.join()` trên chúng. Nhưng hình dạng THẬT do `brand-profile-form.tsx` (giao diện đang dùng để lưu) ghi lại là OBJECT (`{default: "..."}` hoặc `{default: [...]}`), không phải mảng/chuỗi trần — gọi `.join()` trên object ném `TypeError` ngay lập tức. Đây là lỗi vỡ thật cho BẤT KỲ tổ chức nào đã nhập hồ sơ thương hiệu qua giao diện thật rồi bấm "Sinh dữ liệu bán hàng" (M01b, tính phí credit) — không phải một trường hợp biên hiếm gặp. Đã sửa: thêm hai hàm thuần `extractBrandHashtags()`/`extractBrandCtaPhrase()` vào `profile-rules.ts` (đọc an toàn cả hai hình dạng, trả `null` cho hình dạng lạ thay vì đoán), dùng ở `generate-product-copy.ts` thay ép kiểu; đổi kiểu `ProductCopyInput.brand.cta_templates` từ `string[] \| null` sang `string \| null` (đúng nghĩa MỘT câu CTA theo nợ #102) và bỏ `.join(" \| ")` không còn áp dụng được; sửa luôn `tai-anh/page.tsx` (`ctaPhrases` từng kiểm `Array.isArray()` — sai, không bao giờ khớp dữ liệu thật) dùng cùng hàm đọc. Nhân tiện phát hiện và sửa luôn 2 lỗi `tsc` không liên quan (fixture `flower-consultant-rules.test.ts`, cả hai bản, thiếu ba trường bắt buộc `variants`/`galleryImages`/`warningTags` của `ProductMasterIndex` từ nợ #95 — mảng rỗng, không phải quyết định nghiệp vụ) | Phát hiện khi soát tiếp phạm vi nợ #99 (nghiên cứu template override), không phải phạm vi ban đầu — nhưng mức độ nghiêm trọng (crash một tính năng tính phí credit cho khách hàng thật) buộc phải sửa ngay thay vì ghi nợ rồi để đó, đúng nguyên tắc an toàn dữ liệu của AGENTS.md | — Đã trả. Xác minh 17/09 trên máy anh Tony: `npx tsc --noEmit` sạch, `npm test` **493/493 xanh, 67 tệp test** (gồm 6 ca mới cho `extractBrandHashtags`/`extractBrandCtaPhrase` trong `profile-rules.test.ts`) |

Ghi ngày 17/09 (tích hợp IOPaint tự host cho AIC-13 — Mức 2, tiếp nợ #78, chốt qua đánh giá đặc tả bên ngoài + hai quyết định AskUserQuestion với người dùng: giữ Mức 2 ngay dù lệch thứ tự khuyến nghị của §2, và giữ AIC-13 nested trong M04b thay vì tách capability riêng):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 104 | **GIAI ĐOẠN 1 (CƠ CHẾ + MASK) ĐÃ DỰNG VÀ CÓ TEST 17/09, CHƯA CHẠY TRÊN GPU/MODEL THẬT.** `ImageExpander.expand()` (`providers/base.py`) nay nhận thêm `product_mask: bytes \| None = None` (lùi về tương thích ngược — `PadExpander`/`ReplicateOutpainter` bỏ qua tham số, không đổi hành vi). Hiện thực thứ ba `IOPaintExpander` (`providers/expansion/iopaint_outpainter.py`) gọi IOPaint tự host qua `POST /api/v1/inpaint` với `use_extender=true` — đăng ký `"iopaint"` trong `EXPANDER_REGISTRY` (`providers/expansion/router.py`). `variant_worker.py` dựng `product_mask` từ CHÍNH alpha đã có của `RembgSegmenter` (`_mat_na_iopaint`, không chạy segmentation lần hai) và DÁN LẠI CHÍNH XÁC `rgba` gốc lên ảnh engine trả về TRƯỚC khi đóng khung (`_dan_lai_chu_the`, đọc `paste_offset` mà engine tự báo — không đoán vị trí). `IOPAINT_URL`/`IOPAINT_MODEL`/`IOPAINT_DEVICE`/`IOPAINT_TIMEOUT_SECONDS` thêm vào `.env`/`.env.example`, để trống thì lùi về `PadExpander` như `ReplicateOutpainter` không token. 14 ca thử mới (`test_iopaint_outpainter.py` — MockTransport httpx, không gọi mạng thật) + 6 ca mới trong `test_variant_worker.py` khoá đường lùi-về-an-toàn, hình dạng request, `toa_do_extender()` (hàm thuần), và đường nối `product_mask → expand() → paste-back → dong_khung` bằng một engine giả lập — toàn bộ `workers/tests/` (76 ca) xanh sau đợt sửa. Toạ độ `extender_x/y/width/height`/`paste_offset` đối chiếu trực tiếp với mã nguồn thật `iopaint==1.6.0` (`model/base.py::_do_outpainting`), không suy đoán. **Phát hiện mới khi đọc mã nguồn đó (ngoài phạm vi "Xác minh IOPaint" đã chốt trong tài liệu tích hợp):** khi `use_extender=true`, IOPaint tự tính mask NỘI BỘ từ hình chữ nhật `extender_*` — mask do CLIENT gửi (`product_mask`) bị BỎ QUA HOÀN TOÀN ở đường outpainting (chỉ được dùng ở đường inpaint thường, không `use_extender`). Nghĩa là phép đo pixel-diff=0 đã làm trong tài liệu tích hợp (dùng LaMa, đường inpaint thường) KHÔNG áp dụng cho đường outpainting thật sẽ chạy ở Giai đoạn 2 — bảo vệ sản phẩm ở đó đến từ (a) cơ chế giữ-nguyên-toàn-ảnh-gốc của chính `use_extender` và (b) bước dán lại chính xác `_dan_lai_chu_the`, KHÔNG phải từ `product_mask` gửi lên. `docker/iopaint/Dockerfile` + `README.md` viết dựa trên CLI/phụ thuộc thật của `iopaint==1.6.0` (không dùng lại `docker/CPUDockerfile`/`GPUDockerfile` cũ của repo IOPaint — đã xác minh còn trỏ tên gói `lama-cleaner`) nhưng **CHƯA build/chạy thử** — môi trường soạn mã không có GPU. **Còn nguyên, KHÔNG làm được từ môi trường soạn mã:** (1) chọn + soát giấy phép một model SD/SDXL-inpainting thật có `support_outpainting=true` (LaMa mặc định của IOPaint KHÔNG hỗ trợ — đã xác minh dùng `use_extender` với LaMa trả 200 OK nhưng không mở rộng canvas); (2) build/chạy Dockerfile trên máy có GPU thật; (3) chạy `IOPaintExpander` thật qua `variant_worker.py` trên ảnh hoa thật, đo `_do_lo_chu_the` có còn ≥ 0,99 khi bảo vệ sản phẩm đến từ dán-lại chứ không phải mask; (4) production hardening (retry mạng có giới hạn, log chi phí thật) và benchmark qua `golden`/`chay-du-bo-may.py`; (5) chủ sở hữu hạ tầng GPU — **đã chốt 18/09: laptop cá nhân (MacBook Air M3) cho giai đoạn phát triển, chuyển sang VPS khi lên production**, xem tài liệu tích hợp. Đây đúng là Giai đoạn 2-5 của kế hoạch 5 giai đoạn trong tài liệu tích hợp — không phải thiếu sót, là ranh giới thật của môi trường soạn mã (không GPU, mạng ra ngoài bị chặn ở cấp tổ chức nên không tự tải model/đối chiếu giấy phép sâu được). **Lưu ý riêng cho laptop Apple Silicon (M3):** GPU của máy này là Apple GPU (Metal/`mps`), KHÔNG phải NVIDIA/CUDA — Docker Desktop trên macOS không pass-through được Metal vào container Linux, nên chạy `IOPaintExpander` qua `docker/iopaint/Dockerfile` trên máy này sẽ chỉ chạy CPU dù máy có GPU thật. Muốn dùng GPU của M3 để thử nhanh (chậm hơn NVIDIA nhưng nhanh hơn CPU nhiều) phải cài `iopaint` trực tiếp bằng `pip` (không qua Docker) và chạy `iopaint start --device mps` | Quyết định 17/09 (AskUserQuestion): tích hợp Mức 2 ngay dù tài liệu kiến trúc khuyến nghị thứ tự API-trước (§2) — chấp nhận đảo thứ tự vì lý do ngoài kiến trúc thuần (kiểm soát dữ liệu ảnh khách hàng, chi phí dài hạn), không phải vì Mức 1 đã đo là không đủ. Dựng đủ cơ chế/codebase + khoá bằng test trước, chờ GPU thật để xác minh phần còn lại — cùng khuôn quyết định đã áp dụng cho `ReplicateOutpainter` ở nợ #78 | Khi có máy GPU thật (hoặc thử nhanh bằng `mps` trên laptop): build `docker/iopaint/Dockerfile` (VPS/NVIDIA) hoặc `pip install iopaint` (laptop M3), chọn + soát giấy phép model SD/SDXL-inpainting, chạy thử `IOPaintExpander` qua `expand_provider="iopaint"` trên ảnh hoa thật, đo `_do_lo_chu_the`, rồi mới bật cho khách hàng — xem kế hoạch 5 giai đoạn đầy đủ trong tài liệu "Tích hợp IOPaint Outpainting vào floraos-core" |

Ghi ngày 18/09 (cập nhật nợ #104 — chọn xong model + giấy phép, chuẩn bị đường chạy native trên Apple Silicon):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 104 | **Mục (1) của nợ #104 ĐÃ XONG — model chọn xong, giấy phép sạch.** Model outpainting mặc định: `kandinsky-community/kandinsky-2-2-decoder-inpaint` (kèm `kandinsky-community/kandinsky-2-2-prior` mà `AutoPipelineForInpainting` tự tải) — cả hai giấy phép **Apache-2.0**, xác nhận trên trang model HuggingFace của từng model (không suy đoán từ tên), không ràng buộc thương mại. `support_outpainting=true` xác nhận đọc thẳng `iopaint/schema.py` (`iopaint==1.6.0`, dòng 88-96): Kandinsky 2.2 whitelist riêng theo TÊN model (`self.name in [KANDINSKY22_NAME, POWERPAINT_NAME]`), tách khỏi nhánh theo họ SD/SDXL — kiến trúc Kandinsky khác SD/SDXL nên cần whitelist riêng, không suy đoán từ `model_type`. `runwayml/stable-diffusion-inpainting` (CreativeML Open RAIL++-M) vẫn CHƯA soát xong (model card ghi "chỉ cho nghiên cứu", điều khoản license thật cho phép thương mại có điều kiện — mập mờ này là lý do chưa dùng, không phải license cấm) — để ngỏ làm lựa chọn thay thế sau khi soát xong, vì ảnh SD/SDXL-inpainting thường chân thực hơn Kandinsky cho ảnh sản phẩm. Đã cập nhật `docker/iopaint/Dockerfile` (`ENV MODEL=` đặt sẵn Kandinsky 2.2), `docker/iopaint/README.md`, `.env.example` theo quyết định này. Thêm `docker/iopaint/run_native_mps.sh` — cài `iopaint==1.6.0` bằng `pip` vào venv riêng (không qua Docker, không đụng venv của `workers/`) và chạy `iopaint start --device mps`, vì Docker Desktop trên macOS không pass-through Metal (đã ghi ở bản 17/09 của nợ này). **Mục (2)-(4) của nợ #104 vẫn còn nguyên — lần này CŨNG không làm được từ phiên làm việc này:** `device_bash` (kênh chạy lệnh trên máy anh Tony qua cầu nối thiết bị) báo "Workspace unavailable — the isolated Linux environment on this device failed to start" ở cả ba lần thử (17/09 và 18/09) — không phải vì máy thiếu GPU, mà vì chính kênh chạy lệnh từ xa trên máy đó chưa khởi động được. `run_native_mps.sh` vì vậy viết SẴN để anh Tony tự chạy trên máy, chưa được chạy thật bởi phiên làm việc này | (kế thừa nợ #104) — chọn xong model và soát giấy phép không cần chờ có GPU; việc chạy thật vẫn đợi kênh chạy lệnh trên máy thật hoạt động hoặc anh Tony tự chạy script | Khi anh Tony chạy `docker/iopaint/run_native_mps.sh` trên máy (hoặc `device_bash` khởi động lại được): xác nhận `curl http://127.0.0.1:8080/api/v1/model` trả `support_outpainting: true`, nối `IOPAINT_URL=http://127.0.0.1:8080`/`IOPAINT_MODEL=kandinsky-community/kandinsky-2-2-decoder-inpaint`/`IOPAINT_DEVICE=mps` vào `.env`, chạy `IOPaintExpander` thật qua `expand_provider="iopaint"` trên ảnh hoa thật, đo `_do_lo_chu_the`, đo thời gian khởi động/dung lượng tải model |

Ghi ngày 18/09 (đảo quyết định nợ #104 — dừng Mức 2 làm mặc định, quay lại thứ tự API-trước mà tài liệu kiến trúc §2 khuyến nghị từ đầu, sau khi gặp ma sát hạ tầng thật ngay ở lần thử đầu tiên):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 104 | **DỪNG Ở ĐÂY — Mức 2 (IOPaint tự host) không tiếp tục làm đường mặc định của AIC-13.** Lần chạy thật đầu tiên trên máy phát triển (MacBook Air M3, native `mps`, model Kandinsky 2.2, script `run_native_mps.sh`) gặp liên tiếp ba lớp ma sát hạ tầng thật trong cùng một buổi: (1) Python mặc định của máy (3.14) không tương thích bảng phụ thuộc của `iopaint==1.6.0` (`Pillow==9.5.0` không có wheel cho 3.13+, phải dò riêng một bản Python 3.9-3.12); (2) tải trọng số model (Kandinsky 2.2 gồm cả decoder lẫn prior, nhiều GB) làm hết dung lượng đĩa còn trống của máy giữa chừng (`OSError: [Errno 28] No space left on device`); (3) mỗi lần đổi máy hoặc đổi model phải tải lại từ đầu. Đây đúng là loại chi phí vận hành mà tài liệu kiến trúc §2 đã nêu khi khuyến nghị thứ tự API-trước — nay đã thấy TRỰC TIẾP trên máy thật, không còn là rủi ro lý thuyết. Toàn bộ mã Giai đoạn 1 (`IOPaintExpander`, `product_mask`, `_dan_lai_chu_the`, 20 ca thử mới) VẪN GIỮ NGUYÊN trong `EXPANDER_REGISTRY`, không xoá — chỉ không còn là lựa chọn mặc định, bật lại bất kỳ lúc nào qua `expand_provider="iopaint"` khi có hạ tầng GPU thật (VPS, ổn định hơn laptop cá nhân). Provider khuyến nghị dùng cho lượt gọi thương mại tiếp theo của AIC-13 là `fal_bria_expand` qua fal.ai (nợ #105, mở mới cùng ngày) — LƯU Ý: `DEFAULT_EXPANDER` của `router.py` vẫn là `"pad"` như từ đầu (nợ #80 — không tự động gọi provider tốn tiền khi job không yêu cầu rõ), "khuyến nghị" ở đây nghĩa là tên truyền vào `expand_provider=` khi một job MUỐN sinh nội dung mới ở biên, không phải một hành vi tự động mới | Ma sát hạ tầng thật (Python, dung lượng đĩa, băng thông tải model) phát sinh ngay ở bước THỬ ĐẦU TIÊN trên laptop phát triển, trước khi tới VPS thật — xác nhận đúng rủi ro tài liệu kiến trúc §2 đã nêu khi khuyến nghị API-trước. Tiếp tục đẩy qua VPS trước khi có bằng chứng khác sẽ lặp lại đúng ma sát này ở quy mô lớn hơn (chi phí thuê máy GPU) mà chưa chắc đổi lại chất lượng ảnh tốt hơn provider thương mại | Khi volume đủ lớn để chi phí self-host GPU rẻ hơn gọi API theo lượt (so với chi phí Mức 1 mới đo được thật), quay lại bật `expand_provider="iopaint"` — mã đã sẵn, chỉ cần hạ tầng |

Ghi ngày 18/09 (nợ #105 — thêm `FalAIOutpainter`, hiện thực `ImageExpander` thứ tư, chốt qua AskUserQuestion 18/09: chỉ đổi provider outpaint, tái dùng nguyên `ImageExpander`/`EXPANDER_REGISTRY` đã có — KHÔNG dựng cổng chung mới cho upscale/remove_bg/enhance):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 105 | **DỰNG XONG, CHƯA CHẠY THẬT (chưa có `FAL_KEY`).** `FalAIOutpainter` (`providers/expansion/fal_outpainter.py`) gọi `fal-ai/bria/expand` qua fal.ai — hàng đợi REST chuẩn (`POST queue.fal.run/...` → poll `.../status` tới `COMPLETED` → `GET .../requests/{id}` lấy `image.url` → tải ảnh), đăng ký `"fal_bria_expand"` trong `EXPANDER_REGISTRY`. Lược đồ input/output xác minh qua tài liệu chính thức fal.ai (không suy đoán, 18/09): `image_url` (data URI base64 — fal khuyến cáo chỉ dùng cho ảnh vài KB, ảnh sản phẩm thật lớn hơn nhiều, đây là đánh đổi đã biết chưa đo được; đúng hơn là upload CDN trước nhưng endpoint REST thô của bước đó không được tài liệu hoá ổn định), `canvas_size`/`original_image_size`/`original_image_location` — ba trường hình học CÙNG phép đặt-giữa-canvas với `toa_do_extender()` của IOPaint (`toa_do_bria()`, hàm thuần, có test) nhưng KHÔNG đổi dấu tọa độ (khác `extender_x/y` âm của IOPaint). `original_image_location` trả qua `parameters["paste_offset"]` — `variant_worker.py::_dan_lai_chu_the` dùng lại y nguyên, KHÔNG cần sửa gì ở `variant_worker.py`: đây là bằng chứng nguyên tắc `ImageExpander` (nợ #78) đã đủ để đổi provider mà không đụng pipeline, đúng ý kiến trúc "Image AI Gateway" người dùng mô tả 18/09 — cổng đó đã tồn tại dưới tên `ImageExpander`/`EXPANDER_REGISTRY`, KHÔNG phải xây mới. Không có trường mask nào trong lược đồ (giống `bria/expand-image` trên Replicate, cùng hãng model) — `product_mask` bị bỏ qua có chủ đích, bảo vệ sản phẩm đến từ `_dan_lai_chu_the`, không phải mask gửi lên. Giá `$0.04/lượt` xác nhận trực tiếp trên trang model fal.ai (18/09), thêm vào `gia_luot_replicate["fal-ai/bria/expand"]` (`contracts/config.json`). 12 ca thử mới (`test_fal_outpainter.py`, MockTransport httpx, không gọi mạng thật) khoá đường lùi-về-an-toàn (thiếu key/lỗi mạng/không bao giờ `COMPLETED`/response thiếu `image.url`), hình dạng request (endpoint, header `Authorization: Key` — KHÔNG phải `Bearer` như Replicate, ba trường hình học), và `toa_do_bria()`. `FAL_KEY`/`FAL_TIMEOUT_SECONDS` thêm vào `.env`/`.env.example`, để trống thì lùi về `PadExpander` như hai provider kia. Toàn bộ `workers/tests/` (88 ca) xanh sau đợt thêm. **Còn nguyên, không làm được từ môi trường soạn mã (chưa có `FAL_KEY`):** gọi thử thật, xác nhận đúng tên trường/cách gửi ảnh (đặc biệt data URI có chạm giới hạn kích thước request cho ảnh sản phẩm thật không), đo `_do_lo_chu_the` trên ảnh hoa thật, đối chiếu lại giá `$0.04` sau vài trăm lượt gọi thật (giá niêm yết có thể khác giá tính thật nếu có phụ phí độ phân giải) | Quyết định 18/09 (AskUserQuestion): chỉ đổi provider outpaint, không dựng cổng chung 5 khả năng — vì `ImageExpander`/`EXPANDER_REGISTRY` (nợ #78) ĐÃ LÀ đúng lớp trừu tượng đó cho riêng outpaint, dựng thêm là trùng lặp không cần thiết ở phạm vi hiện tại | Khi có `FAL_KEY`: nối vào `.env`, chạy thử `FalAIOutpainter` thật qua `expand_provider="fal_bria_expand"` trên ảnh hoa thật, đo `_do_lo_chu_the`, xác nhận lược đồ request đúng như tài liệu (sửa `_goi_queue`/`_tai_anh_ra` nếu fal.ai trả lỗi 4xx do sai tên trường), rồi mới bật cho khách hàng |

Ghi ngày 18/09 (rà soát toàn bộ trạng thái bảy năng lực M04b AIC-11→17, theo yêu cầu đối chiếu kiến trúc toàn hệ thống — không chỉ riêng AIC-13):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 106 | AIC-14 `image_retouch_deterministic` (sáng/tương phản/màu tự động) và AIC-15 `image_retouch_generative` (sửa ảnh sinh AI) — cả hai đặc tả trong `docs/dac-ta/10-ai-orchestration.md` (bảng năng lực, thuộc M04b) — KHÔNG CÓ MỘT DÒNG MÃ nào trong `workers/media_ai/jobs/variant_worker.py` hay bất kỳ đâu khác trong phạm vi M04b: không hàm, không route, không test. Rà bằng `grep -rln "retouch\|brightness\|contrast"` trên `workers/`/`src/` chỉ khớp các hàm tăng cường của M04a (`studio_enhancer.py`, `openai_enhancer.py`), không liên quan AIC-14/15. `CHECKLIST_AI_CAPABILITIES_BUILD.md` mục 3.4 trước đợt sửa này ghi cả hai 🔄 ("đang làm") — sai, đã sửa về đúng trạng thái chưa bắt đầu | Các đợt P16 (09/12) và P24 (09/17) đều tập trung đúng phạm vi đã chốt: xoá nền, đổi nền, watermark, biến thể kênh — retouch chưa từng nằm trong phạm vi một đợt cụ thể nào, không phải việc bị bỏ sót giữa chừng | Khi chủ sản phẩm chốt đưa AIC-14/15 vào phạm vi một đợt: AIC-14 là hàm thuần PIL không cần model AI (cùng dạng AIC-16 `dong_dau()`); AIC-15 cần qua cổng `ImageProvider.edit()` đã có stub TS (`src/core/ports/image-provider.ts`, xem tài liệu tích hợp IOPaint) nhưng cổng đó `import` một tệp không tồn tại (`./shared-media`) — sửa cùng lúc |
| 107 | **PHÁT HIỆN + TỰ SỬA 18/09.** `TRANG_THAI.md` dòng 111 ghi "P16+ — M04a & M04b AI Creative Studio hoàn thiện 100% Production & Commercial Ready (09/14)", mô tả route `POST /api/v1/media/background-removal` (`process_m04b_variants.py`) và một "engine client fallback" dựng canvas phía trình duyệt kèm ảnh mẫu Unsplash. Đúng ba ngày sau, chính tệp này ở mục P24 (09/17, dòng 21-26) xác nhận CHÍNH route đó không đăng nhập, không RBAC, không gắn tổ chức, không trừ credit, `spawn` Python rồi đọc stdout (trái luật `AGENTS.md`), và có lỗ SSRF qua `image_url` — phải đóng hẳn; đồng thời gỡ bộ dựng canvas phía trình duyệt/ảnh mẫu Unsplash khỏi chuỗi lùi. Tuyên bố "100% Production & Commercial Ready" chưa từng được đính chính dù kiến trúc nó mô tả đã bị chính tệp phủ nhận bốn ngày sau | Tệp này là nhật ký theo ngày, không xoá/viết đè entry cũ — nên dòng 09/14 vẫn đứng nguyên văn dù đã sai, dễ khiến người đọc sau (hoặc một phiên AI khác chỉ đọc lướt) tưởng nhầm M04b đã 100% sẵn sàng thương mại từ 09/14, trong khi rà lại 18/09 xác nhận AIC-14/AIC-15 (nợ #106) chưa có mã, và AIC-13 (nợ #78/#104/#105) chưa route nào gọi tới được | — Đã trả 18/09: thêm dòng đính chính ngay dưới entry 09/14 trong `TRANG_THAI.md`, trỏ sang mục P24 và bảng trạng thái bảy năng lực thật trong `CHECKLIST_AI_CAPABILITIES_BUILD.md` §3.4 |

Ghi ngày 18/09 (cập nhật nợ #106 — ba quyết định phạm vi/giá chốt qua AskUserQuestion với anh Tony, trước khi viết bất kỳ dòng mã nào cho AIC-14/15/17 mở rộng):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 106 | **CHỐT PHẠM VI 18/09 — tách đôi nợ #106 gốc.** (1) **AIC-15 chuyển hẳn sang M04a**, không còn thuộc M04b: đúng luật đã ghi ở `BO_TINH_NANG_HIEN_TRANG.md` dòng 234 ("sửa ánh sáng hay hình dáng bó hoa là một lượt M04a mới, có Guard và cổng duyệt riêng") — đi qua Identity Guard sẵn có, không dựng cổng đo mới, không đụng tới cổng Subject Integrity của M04b. (2) **AIC-14 giữ ở M04b nhưng khoanh phạm vi**: chỉ chỉnh sáng/tương phản vùng NỀN đã ghép (`StudioBackdropEngine`), không chạm pixel bó hoa — tương thích thẳng cổng Subject Integrity hiện có (pixel-diff = 0 trong alpha mask chủ thể), không cần sửa `variantIntegrityResult()`. Đã cập nhật `CHECKLIST_AI_CAPABILITIES_BUILD.md` — AIC-15 chuyển sang mục 3.3 (M04a), AIC-14 ghi rõ phạm vi ở mục 3.4 (M04b) | Quyết định 18/09 (AskUserQuestion, 3 câu hỏi cùng lúc): AIC-14 chọn "chỉ chỉnh phông nền" (an toàn, làm được ngay, không sửa luật đo) thay vì "chỉnh toàn ảnh kể cả hoa" (đẹp hơn nhưng phải đổi luật đo Subject Integrity, rủi ro cao hơn); AIC-15 chọn "M04a" thay vì giữ ở M04b (giữ ở M04b sẽ phải dựng cổng đo mới xung đột thẳng với nguyên tắc nền "M04b không sinh pixel mới trên bó hoa") hoặc để ngỏ chưa chốt | Kế hoạch kỹ thuật chi tiết cho cả hai đã có ở tài liệu Claude Docs "Kế hoạch mở rộng M04b: 30 biến thể · chỉnh sáng tự động · sửa ảnh AI" mục 3 (AIC-14) và mục 4 (AIC-15, dù tên mục vẫn ghi trong ngữ cảnh M04b) — viết mã theo đúng thứ tự đã liệt ở đó |
| 108 | **CHỐT GIÁ 18/09** cho đường chạy lô nhiều biến thể (mở rộng nợ #86, hướng tới 30 biến thể/lượt xuất bộ). Giữ nguyên cách tính giá hiện tại: mỗi job vẫn 1 credit như hôm nay, không thêm logic giá gói/giảm giá theo lô. Chạy lô 20-24 job (theo kế hoạch mục 2 của tài liệu "Kế hoạch mở rộng M04b") tính đúng 20-24 credit cho một lần xuất cả bộ, không có chiết khấu | Quyết định 18/09 (AskUserQuestion): chọn "tính như hiện tại" thay vì "giảm giá theo gói" (cần thêm logic tính giá gói, chưa cấp thiết) hoặc "chưa chốt, để sau" — giữ đơn giản, không thêm việc ngoài phạm vi cho tới khi có nhu cầu thật từ một tiệm hoa dùng tính năng chạy lô | Nếu volume thật cho thấy khách hàng phàn nàn giá chạy lô quá cao so với mua lẻ, quay lại bàn logic giá gói — không làm trước khi có bằng chứng nhu cầu, đúng nguyên tắc đã áp dụng cho nợ #86 |

Ghi ngày 18/09 (triển khai mã cho AIC-14 và chạy lô 24 biến thể — tiếp nợ #106/#108, tự sửa một khẳng định sai còn sót trong nợ #106 gốc):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 106 | **TỰ SỬA MỘT KHẲNG ĐỊNH SAI TRONG NỢ #106 GỐC + XÂY XONG AIC-14.** Nợ #106 gốc ghi "`ImageProvider.edit()` `import` một tệp không tồn tại (`./shared-media`)" — SAI. Đọc trực tiếp `src/core/ports/image-provider.ts` và `src/core/ports/shared-media.ts` xác nhận `shared-media.ts` TỒN TẠI, đúng hình dạng (`AssetRef`/`Box`/`Point`/`MaskData`/`ProviderMedia`); `npx tsc --noEmit` trên toàn repo sạch, không báo lỗi import nào ở hai tệp này. Điều còn ĐÚNG từ nợ gốc: `grep -rln "ImageProvider" src/` (trừ chính tệp cổng) không khớp nơi nào khác — cổng vẫn CHƯA được nối vào bất kỳ use-case/worker thật nào (đúng cho AIC-15, nay đã chuyển hẳn sang M04a theo quyết định 18/09 ở trên). **AIC-14 đã xây, có test, đúng phạm vi "chỉ nền":** hàm thuần `tu_dong_can_bang_sang()` (`workers/media_ai/image/auto_retouch.py`, `PIL.ImageOps.autocontrast`) không biết gì về chủ thể/mặt nạ; an toàn đến từ hàm nối `_ap_dung_auto_enhance()` (`variant_worker.py`) — tăng sáng TOÀN khung rồi dán lại NGUYÊN KHỐI chủ thể gốc (chưa qua tăng sáng) đè lên, offset `(0,0)`, cùng nguyên tắc belt-and-suspenders với `_dan_lai_chu_the` (AIC-13). Bật qua `auto_enhance: boolean` (mặc định `false`, không đổi hành vi cũ) trong payload job, nối xuyên `POST /media/variants` và `POST /media/variants/batch` (route mới, xem nợ #108 bên dưới). Đánh đổi có chủ đích đã ghi trong docstring `_ap_dung_auto_enhance`: bật cờ này thay viền đã làm mềm/light-wrap của `composite()` bằng viền thô của chủ thể gốc cho đúng lượt đó — ưu tiên an toàn hơn giữ độ mượt viền. 6 ca thử mới (`TestAutoEnhance` trong `test_variant_worker.py`, `TestTuDongCanBangSang` trong `test_auto_retouch.py` mới) khoá đúng bất biến "mọi pixel vùng alpha > 0 của chủ thể trùng tuyệt đối trước/sau" bằng so khớp mảng, không phải ngưỡng gần đúng | Khẳng định sai bắt nguồn từ việc kế thừa một nhận định chưa tự xác minh lại trong một phiên làm việc trước — đọc thẳng tệp thật (thay vì tin lại ghi chú cũ) mới lộ ra. AIC-14 nằm đúng trong danh mục ưu tiên đã chốt (mục 3 trong 5 việc cần làm, xếp trước AIC-15) | — Đã trả (mã + test). `npx tsc --noEmit` sạch, `npx eslint` sạch cho các tệp đổi, `npx vitest run` (loại `tests/tenant/**`) qua các module `src/modules/media` xanh. Python: 25/25 test liên quan (`test_variant_worker.py` + `test_auto_retouch.py` mới) xanh trên máy anh Tony qua `device_bash`, trừ 1 ca gọi `RembgSegmenter` thật (đã có từ trước, không phải ca mới) không chạy được trong chính phiên VM sandbox (không đủ RAM tải model ONNX, không phải lỗi mã — xem chi tiết ở nợ #108) |
| 108 | **DỰNG XONG CƠ CHẾ CHẠY LÔ 18/09 (`POST /media/variants/batch`) — CHƯA CHẠY ĐƯỢC MIGRATION TRÊN DATABASE DEV THẬT TỪ PHIÊN NÀY.** `generation_jobs.job_group_id String?` (nullable, không đổi hành vi job đơn lẻ) + `@@index([organization_id, job_group_id])` đã thêm vào `prisma/schema.prisma` trên máy anh Tony. Nối xuyên `GenerationJobRepository.create()` → `PostgresQueueProvider.enqueue()` → `enqueueJob()` → `requestVariantBatch()` (mới, `request-variants.ts`): MỖI job trong lô vẫn qua NGUYÊN `enqueueJob()` — vẫn `idempotency_key` riêng (`variantBatchIdempotencyKey()`, dẫn xuất từ khoá gốc client gửi qua header, nối `:preset:ratio`), vẫn trừ credit riêng từng job đúng chốt "tính như hiện tại" (không giảm giá theo lô). `ALL_VARIANT_COMBINATIONS` (`variant-rules.ts`) liệt đủ ma trận 6 preset × 4 ratio = 24 tổ hợp, thứ tự cố định. Route mới `POST /media/variants/batch` (`I4`) nhận `presets`/`ratios` (mảng cụ thể hoặc `"all"`, mặc định `"all"`). Worker (`variant_worker.py`) thêm cache tách chủ thể theo ĐĨA, khoá bằng `master_asset_id` (`_doan_chu_the`, thư mục `var/storage/cache/m04b_segmentation/`) để 24 job của CÙNG một Master Image không chạy lại model segmentation (bước tốn thời gian nhất) 24 lần — mặc định TẮT khi không truyền `master_asset_id`/`cache_dir` nên 17 ca thử cũ của `dung_bien_the()` không đổi hành vi. **KHÔNG chạy được `prisma migrate dev`/`prisma generate` thật từ phiên này:** proxy egress của sandbox chặn `binaries.prisma.sh` (403 Forbidden) — đây là engine Prisma cần tải để đọc/áp schema; `npm`/PyPI vẫn vào bình thường, đây là chính sách chặn theo tên miền phía tổ chức, KHÔNG tự ý tìm đường vòng (không tắt proxy, không tải engine qua kênh khác). Đã yêu cầu anh Tony tự chạy `npx prisma migrate dev --name add_job_group_id_to_generation_jobs` trực tiếp trong Terminal thật trên máy (ngoài sandbox, có mạng đầy đủ) — lệnh đó tự sinh đúng SQL từ chính thay đổi schema đã có sẵn và tự chạy `prisma generate` sau. `npx tsc --noEmit` sạch dù Prisma Client CHƯA generate lại (TypeScript không kiểm tra excess-property khi giá trị truyền qua một hàm trung gian như `scopedData()`, nên biên dịch sạch — ở RUNTIME, Prisma Client thật sẽ báo lỗi "Unknown argument `job_group_id`" cho tới khi anh Tony chạy migrate/generate, đây KHÔNG phải bằng chứng mã đã chạy được thật). 4 ca thử mới cho ma trận/khoá idempotency (`variant-rules.test.ts`) xanh; 3 ca thử mới cho cache tách chủ thể (`TestDoanChuThe` trong `test_variant_worker.py`, dùng segmenter giả lập — không cần model thật) xanh. Toàn bộ 280 ca thử Python khác trong `workers/tests/` (trừ hai ca gọi `RembgSegmenter`/model thật — `test_studio_pipeline.py::test_rembg_segmenter_returns_rgba_and_alpha` và 1 ca trong `test_variant_worker.py`, cả hai đều VỐN ĐÃ gọi model thật TRƯỚC đợt sửa này, xác nhận bằng cách gọi lại riêng đúng dòng đó độc lập — cùng bị VM sandbox giết vì hết RAM khi tải model ONNX cả trước lẫn sau khi sửa, không liên quan gì tới thay đổi của đợt này) đều xanh | Batch là phần mở rộng của cùng luồng `enqueueJob` đã kiểm ở nợ #86/#3, không phải một cổng hạn mức/credit mới — rủi ro chính chỉ là migration DB, không phải logic nghiệp vụ. Giới hạn mạng của sandbox (chặn `binaries.prisma.sh`) là ranh giới hạ tầng thật, không phải thiếu sót của đợt sửa này | Khi anh Tony chạy `npx prisma migrate dev --name add_job_group_id_to_generation_jobs` trên máy thật: xác nhận cột/`index` mới có trong Postgres (`\d generation_jobs` hoặc `prisma db pull --print`), `npx prisma generate` xong, chạy lại `npx tsc --noEmit` (phải vẫn sạch — không có gì để sửa thêm nếu đúng), rồi gọi thử `POST /media/variants/batch` thật trên một Master Image đã duyệt, xác nhận 24 job cùng `job_group_id`, đúng 24 credit bị trừ, và cache tách chủ thể chỉ chạy segmentation một lần cho cả lô (đo qua log/thời gian, không đoán) |

Ghi ngày 18/09 (đóng nốt phần chặn của nợ #108 — anh Tony tự chạy migration trên máy thật):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 108 | **ĐÃ TRẢ 18/09.** Anh Tony tự chạy `npx prisma migrate dev --name add_job_group_id_to_generation_jobs` trong Terminal thật trên máy (ngoài sandbox, có mạng đầy đủ) — thành công: `prisma/migrations/20260918050805_add_job_group_id_to_generation_jobs/migration.sql` khớp đúng DDL đã soạn (`ALTER TABLE generation_jobs ADD COLUMN job_group_id`, `CREATE INDEX generation_jobs_organization_id_job_group_id_idx`), "Your database is now in sync with your schema". Sau đó chạy riêng `npx prisma generate` (bước tự động của `migrate dev` không tự chạy hoặc lỗi âm thầm lần đầu — phát hiện qua timestamp tệp sinh ra chưa đổi, đã yêu cầu anh Tony chạy lại) — thành công, `src/generated/prisma/models/generation_jobs.ts` nay có `job_group_id: string \| null`. Xác minh lại: `npx tsc --noEmit` sạch, `npx eslint` sạch, `npx vitest run` (loại `tests/tenant/**`) 41/41 xanh cho `src/modules/media`+`src/modules/jobs` | (kế thừa nợ #108) | — Đã trả hoàn toàn. `POST /media/variants/batch` dùng được thật trên database dev |


Ghi ngày 18/09 (phát hiện khi trả lời câu hỏi về thời gian chạy job M04b — worker chạy đơn tiến trình, không tái sử dụng session model):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 109 | **WORKER M04b HIỆN CHỈ MỘT TIẾN TRÌNH, CHẠY TUẦN TỰ TỪNG JOB — KHÔNG TÁI SỬ DỤNG SESSION MODEL SEGMENTATION.** Đọc trực tiếp `package.json` (`worker:media`) và `workers/media_ai/jobs/worker.py::run_worker()`: script chỉ khởi ĐÚNG MỘT tiến trình Python, vòng lặp `while True` đơn luồng — mỗi lượt `claim_next()` lấy đúng MỘT job (ưu tiên cố định M04a > M04b > M04c), xử lý xong mới lấy job kế, ngủ bằng `LISTEN/NOTIFY` khi rảnh. Không đa luồng, không đa tiến trình, không hàng đợi song song. Thêm vào đó, `RembgSegmenter(model_name="bria-rmbg")` được khởi tạo MỚI ngay trong `_doan_chu_the()` (`variant_worker.py` dòng 307) mỗi khi cache đĩa (nợ #108) không trúng (Master Image khác lần trước) — nghĩa là mỗi job có ảnh gốc khác nhau phải nạp lại session `onnxruntime` từ tệp `.onnx` đã cache (`~/.u2net/bria-rmbg.onnx`, ~1GB) vào bộ nhớ TỪ ĐẦU, không giữ session sống giữa các job trong cùng tiến trình. Cache đĩa của nợ #108 chỉ khử trùng lặp khi CÙNG một `master_asset_id` (một lô 24 biến thể của MỘT ảnh) — không giúp gì khi hai job của hai NGƯỜI DÙNG/TỔ CHỨC khác nhau (hai ảnh gốc khác nhau) tới cùng lúc. `rembg` hiện chạy CPU cục bộ trên máy phát triển, không GPU, không dịch vụ inference tách riêng. Hệ quả đo được thật trên máy anh Tony 18/09: một job M04b đầu tiên (kèm tải model lần đầu) mất khoảng 26 giây chỉ riêng bước tải + nạp model; các job sau (đã có `.onnx` trên đĩa) vẫn phải trả giá nạp session mỗi lần đổi ảnh gốc, dù không tải lại qua mạng. Kết luận: hai job của hai người dùng khác nhau gửi cùng lúc xếp hàng tuần tự đúng nghĩa đen — job thứ hai chờ hết TOÀN BỘ job thứ nhất mới bắt đầu, kể cả bước nạp session, không có gì chạy song song | Kiến trúc này dựng cho một tiến trình dev đơn chạy qua `npm run dev:all` trên máy cá nhân (MacBook Air M3), đúng phạm vi build/kiểm test tính năng M04a/M04b tới giờ (nợ #106/#108) — chưa từng đặt ra bài toán nhiều người dùng gửi job đồng thời. `claim_next()` dùng `SELECT ... FOR UPDATE SKIP LOCKED` — cơ chế khoá này BẢN THÂN NÓ đã an toàn để chạy nhiều tiến trình worker song song ngay hôm nay, không cần sửa một dòng mã khoá job nào; giới hạn hiện tại chỉ nằm ở việc `package.json`/`dev:all` mới khởi ĐÚNG MỘT tiến trình, chưa có cấu hình chạy N tiến trình hay pool giữ session ấm | Trước khi lên production nhiều người dùng đồng thời: (a) khởi N tiến trình `worker:media` song song — cơ chế khoá `SKIP LOCKED` đã sẵn sàng, không cần đổi mã nghiệp vụ; (b) đưa `RembgSegmenter`/session model lên cấp module (singleton sống suốt vòng đời tiến trình) thay vì khởi tạo mới mỗi job, để chỉ trả giá nạp session một lần cho mỗi tiến trình thay vì mỗi ảnh; (c) khi chuyển hạ tầng sang VPS thật (đã chốt hướng ở nợ #104 cho IOPaint, áp dụng tương tự ở đây), cân nhắc chạy segmentation trên GPU hoặc tách thành một dịch vụ inference riêng giữ model luôn "ấm" trong bộ nhớ, tránh nạp lại theo từng job |


Ghi ngày 18/09 (phát hiện khi anh Tony báo job M04b treo ở "Ghép bối cảnh & đóng dấu" — đọc lại đúng trình tự `_set_stage` trong `process_variant_job`):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 110 | **NHÃN STAGE SAI VỊ TRÍ: BƯỚC TÁCH CHỦ THỂ THẬT SỰ CHẠY DƯỚI NHÃN "COMPOSING", KHÔNG PHẢI "SEGMENTING".** Đọc trực tiếp `process_variant_job()` (`variant_worker.py` dòng 644-657): `_set_stage(conn, job_id, "SEGMENTING")` được ghi TRƯỚC khi đọc byte Master Image + logo tiệm (I/O nhanh, không phải bước tách chủ thể); ngay sau đó `_set_stage(conn, job_id, "COMPOSING")` được ghi TRƯỚC KHI gọi `dung_bien_the()` — mà chính `dung_bien_the()` mới là nơi `_doan_chu_the()` (tách chủ thể thật, gọi `RembgSegmenter`) chạy (dòng 419). Hệ quả: giao diện (`FLOW_M04B` trong `creative-studio/page.tsx`) đánh dấu "Tách chủ thể khỏi nền" ĐÃ XONG (dấu tick xanh) và hiển thị "Ghép bối cảnh & đóng dấu" đang chạy, trong khi THỰC TẾ bước tách chủ thể (nạp lại session model ~1GB nếu cache-miss, nợ #109) mới là việc đang chạy — người dùng nhìn nhãn sai nên tưởng bước ghép bối cảnh (thuần PIL, đáng lẽ vài giây) bị treo, trong khi đang chờ đúng bước tách nền nặng nhất. Đây là nguyên nhân trực tiếp của câu hỏi "chạy mãi không xong ở Ghép bối cảnh" | Nhãn `stage` được thêm cho MỤC ĐÍCH hiển thị tiến trình thật cho người dùng (thay bản giả lập `setTimeout` cũ, xem comment dòng 78-79 `creative-studio/page.tsx`) — nhưng vị trí ghi `_set_stage("COMPOSING")` bị đặt SỚM HƠN một bước so với công việc thật nó mô tả, một lỗi tách rời thời điểm ghi log khỏi thời điểm thực thi, không phải lỗi tính đúng sai kết quả (biến thể vẫn ra đúng) | Đổi thứ tự: ghi `_set_stage(..., "SEGMENTING")` bọc sát ngay trước lệnh gọi thật gây tốn thời gian (tách `_doan_chu_the()` ra khỏi `dung_bien_the()` để gọi riêng trước, hoặc truyền một callback ghi stage vào giữa `dung_bien_the()`), rồi mới `_set_stage(..., "COMPOSING")` sau khi có `rgba`/`alpha` — để nhãn hiển thị đúng bước đang thật sự chạy |
| 111 | **SEGMENTATION BỊ CHẠY LẶP: M04a ĐÃ TÁCH NỀN MỘT LẦN CHO MASTER IMAGE, M04b TÁCH LẠI TỪ ĐẦU TRÊN CHÍNH ẢNH ĐÓ.** Xác nhận qua mã: `StudioEnhancer` (`studio_enhancer.py`, provider mặc định của M04a) tự khởi `RembgSegmenter()` và gọi tách chủ thể để dựng ảnh Master (composite lên phông Studio mặc định, capability `remove_background` nằm trong `DEFAULT_AUTO_CAPABILITIES`). Nhưng kết quả tách đó (rgba/alpha) KHÔNG được lưu lại — chỉ ảnh Master đã ghép phẳng (RGB, đã có sẵn MỘT phông nền) được ghi làm asset. Khi M04b cần đặt CHÍNH bó hoa đó vào một phông KHÁC (bối cảnh người dùng chọn ở Khu vực B), nó buộc phải tách chủ thể LẠI TỪ ĐẦU trên ảnh Master đã phẳng — không có cách nào lấy lại mask cũ vì mask cũ chưa từng được ghi ra đĩa/DB. Cache đĩa của nợ #108 (`var/storage/cache/m04b_segmentation/`) chỉ được ĐỌC/GHI trong phạm vi worker M04b, không được M04a ghi vào — nên ngay CẢ LẦN CHẠY M04b ĐẦU TIÊN trên một Master Image (không phải một lượt lô nhiều biến thể) cũng không tận dụng được gì từ việc M04a đã tách nền trước đó | Đúng như trực giác anh Tony nêu — đây là công việc tính TRÙNG thật, không phải cảm giác sai. Xảy ra vì M04a và M04b được xây ở hai đợt khác nhau (M04a từ P9, M04b tách nền/biến thể từ P24), mỗi bên tự quản lý vòng đời `RembgSegmenter` của riêng mình, chưa từng có một chỗ ghi mask dùng chung giữa hai module | Mở rộng đúng cơ chế cache đĩa đã có ở nợ #108 sang cả M04a: khi `StudioEnhancer.enhance()` tách chủ thể xong cho một Master Image sắp duyệt, ghi luôn `rgba`/`alpha` vào `var/storage/cache/m04b_segmentation/<master_asset_id>` (cùng định dạng `_duong_dan_cache_chu_the` đang dùng) — khi đó lần gọi `_doan_chu_the()` ĐẦU TIÊN của M04b trên Master Image đó sẽ trúng cache ngay, không tách lại. Cần đối chiếu: ảnh Master đưa vào M04b (`master_bytes`) có đúng là ẢNH GỐC TRƯỚC KHI GHÉP PHÔNG của M04a hay đã là ảnh đã ghép — nếu M04a chỉ lưu ảnh ĐÃ GHÉP PHÔNG làm Master (khả năng cao, do đường dẫn duyệt), việc tái dùng mask vẫn đúng vì tách chủ thể không phụ thuộc phông nền, chỉ phụ thuộc chính chủ thể |


Ghi ngày 18/09 (sửa ngay nợ #110 + mở van tốc độ cho nợ #109 — anh Tony báo job treo, xử lý trong buổi):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 110 | **ĐÃ SỬA 18/09.** `process_variant_job()` không còn tự ghi `stage="COMPOSING"` trước khi gọi `dung_bien_the()`. Thêm tham số `on_stage: Callable[[str], None] \| None = None` vào `dung_bien_the()` — `process_variant_job` truyền `on_stage=lambda stage: _set_stage(conn, job["id"], stage)`; bên trong `dung_bien_the()`, `on_stage("COMPOSING")` chỉ được gọi SAU khi `_doan_chu_the()` (tách chủ thể thật) đã xong. Mặc định `on_stage=None` — mọi lời gọi `dung_bien_the()` cũ trong test không đổi hành vi. Nhãn hiển thị cho người dùng nay khớp đúng việc worker đang thật sự làm | Lỗi chỉ ở ĐIỂM GHI log/stage, không ở logic dựng biến thể — tách callback ra khỏi việc tính toán là cách sửa hẹp nhất, không đụng chữ ký hay hành vi của các bước còn lại | — Đã trả. `pytest tests/media_ai/test_variant_worker.py` (trừ 1 ca gọi model thật đã biết OOM trong sandbox, không liên quan) 21/21 xanh; toàn bộ `tests/media_ai/` (trừ 2 ca gọi model thật đã biết) 135/135 xanh |
| 109 | **MỞ VAN TỐC ĐỘ CHO PHÁT TRIỂN CỤC BỘ (chưa phải trả hết nợ #109 — vẫn còn nguyên phần đa tiến trình + tái sử dụng session).** Thêm biến môi trường `VARIANT_SEGMENTATION_MODEL` (`.env.example`) — để trống thì `_doan_chu_the()` vẫn dùng `bria-rmbg` như cũ (không đổi hành vi/chất lượng mặc định); đặt `u2netp` cho phép dùng model `rembg` nhẹ hơn nhiều khi lặp code cục bộ, đổi lại biên tách kém sắc hơn — KHÔNG dùng cho ảnh khách hàng thật. Đây là van xả tức thời cho việc "chờ quá lâu khi test", không phải bản sửa kiến trúc đầy đủ đã ghi ở nợ #109 gốc (đa tiến trình + giữ session sống) — hai việc đó VẪN CÒN MỞ | Anh Tony đang bị chặn thực tế bởi thời gian chờ khi phát triển — một biến môi trường tuỳ chọn, mặc định giữ nguyên hành vi, là cách an toàn nhất để giải toả ngay mà không động tới đường chạy thật (production vẫn `bria-rmbg`) | (kế thừa nợ #109 gốc) — phần đa tiến trình worker + giữ session `RembgSegmenter` sống giữa các job trong cùng tiến trình vẫn cần làm riêng, chưa đụng tới trong đợt sửa này |


Ghi ngày 18/09 (nợ #112 mới — anh Tony báo job vẫn đứng yên ở SEGMENTING dù đã đổi sang model nhẹ `u2netp`, không rõ nguyên nhân treo thật sự nhưng cần chặn đứng việc treo vô thời hạn ngay):

| # | Nợ | Vì sao chấp nhận | Trả khi nào |
|---|---|---|---|
| 112 | **THÊM HẠN GIỜ CHO BƯỚC TÁCH CHỦ THỂ — CHƯA XÁC ĐỊNH ĐƯỢC NGUYÊN NHÂN TREO GỐC.** `_doan_chu_the()` (`variant_worker.py`) nay chạy `RembgSegmenter.extract_subject` + `EdgeDefringer.defringe` trong một luồng riêng (`ThreadPoolExecutor(max_workers=1)`), giới hạn bằng `VARIANT_SEGMENTATION_TIMEOUT_SECONDS` (mặc định 90 giây). Quá hạn → ném `TimeoutError` rõ ràng, job này bị đánh `FAILED` (nhánh `except Exception` sẵn có của `process_variant_job`) thay vì đứng `PROCESSING` vô thời hạn và chặn đứng mọi job M04b phía sau (hệ quả trực tiếp của kiến trúc một-tiến-trình, nợ #109). 2 ca thử mới (`TestDoanChuTheTimeout`) khoá đúng hai nhánh: quá hạn ném lỗi, không quá hạn vẫn trả kết quả bình thường — dùng segmenter giả lập `time.sleep(5)` với hạn giờ test = 1 giây, xác nhận cơ chế THẬT SỰ hoạt động chứ không chỉ biên dịch sạch. **GIỚI HẠN THẬT của cách sửa này:** Python không có cách buộc dừng một luồng đang chạy — nếu bước tách chủ thể thật sự bị treo (không chỉ chậm), luồng đó vẫn tiếp tục chiếm CPU/bộ nhớ NGẦM sau khi job bị đánh FAILED, cho tới khi tự xong hoặc tiến trình worker được khởi động lại thủ công; đây là cách "phát hiện và báo lỗi sớm", không phải "dừng sạch". **CHƯA XÁC ĐỊNH được vì sao job của anh Tony đứng yên ở SEGMENTING ngay cả sau khi đổi sang `u2netp`** (model tải về chỉ 4.57MB, hoàn tất gần như tức thời theo log terminal) — không loại trừ được từ phiên làm việc này: (a) máy đang chạy đồng thời 6 tiến trình `dev:all` (web, vision, media, video, socialflow, localbudd) tranh CPU; (b) lần khởi tạo `onnxruntime.InferenceSession` đầu tiên cho model mới có chi phí một-lần ngoài dự kiến; (c) một tiến trình `media` cũ từ lần treo trước chưa bị dọn sạch bởi `Ctrl+C`, vẫn giữ tài nguyên. `device_bash` (kênh chạy lệnh từ xa trên máy anh Tony) không thấy được tiến trình Node/Python thật đang chạy trong Terminal của anh Tony (khác không gian tiến trình), nên không tự xác minh được giả thuyết nào ở trên từ phiên làm việc này | Không có cách nào chờ xác định xong nguyên nhân gốc mới sửa — hạn giờ là bước AN TOÀN, ĐÚNG NGUYÊN TẮC "job chết thì phải chết RÕ RÀNG" cần có ngay bất kể nguyên nhân treo là gì | Khi anh Tony xác nhận: (1) sau khi khởi động lại worker với mã mới, job có tự đánh FAILED sau tối đa 90 giây kèm thông báo lỗi rõ ràng hay không (nếu có nghĩa là cơ chế hoạt động đúng, chỉ còn việc tìm nguyên nhân treo gốc); (2) CPU của tiến trình `python`/`media` trong Activity Monitor lúc đang treo — cao (đang tính toán thật, chỉ là chậm) hay gần 0% (kẹt đợi I/O/khoá, đúng nghĩa treo). Từ đó mới thu hẹp được nguyên nhân gốc trong ba khả năng đã liệt ở trên |
| ~~113~~ | **ĐÃ TRẢ 21/09/2026.** Product Intelligence (`POST /market-intelligence/product-intelligence`) nay lưu lại mỗi lượt phân tích vào bảng `product_analysis_runs` mới (TENANT, `organization_id` bắt buộc) — toàn bộ `ProductIntelligenceReport` lưu dạng JSONB, bốn điểm số tách cột riêng để lọc/sắp xếp. Ghi qua `marketIntelligenceRepo.createProductAnalysisRun()`, bọc try/catch an toàn (không chặn phản hồi nếu ghi DB lỗi). Có ca thử cách ly `tests/tenant/product-intelligence-isolation.test.ts` (3 ca: lưu đúng tổ chức, không lộ sang tổ chức khác, mọi bản ghi bắt buộc có `organization_id`) — **cần anh Tony tự chạy `npx prisma generate && npx prisma db push` rồi `npm run test:tenant` trên máy thật để xác nhận runtime**, phiên này không có Postgres/mạng để tự làm | Nội dung nợ gốc giữ lại làm hồ sơ: route trước đây chạy `analyzeProductIntelligence()` trực tiếp và trả JSON, không ghi lại gì — mỗi lần gọi là một lượt phân tích mới, không có lịch sử, không dùng lại được cho Learning Loop hay Product Content Readiness theo dõi theo thời gian (v2.0 mục 26/40). Năm bảng còn lại trong "Product layer" mà v2.0 mục 35 đề xuất (`product_market_matches`, `product_audience_matches`, `product_positioning`, `product_improvement_suggestions`, `product_content_angles`) **vẫn CHƯA XÂY** — không cần thiết cho tối thiểu "phải lưu giữ" (chốt 21/09/2026), vì `report` JSONB đã giữ đủ nguyên trạng | Khi cần tách các bảng con chuẩn hoá (vd. để truy vấn/lọc theo từng loại gợi ý mà không phải parse JSON) — thêm dần theo đúng mẫu v2.0 mục 35 |
| 114 | **Đã GỠ Facebook khỏi Market Intelligence Engine (trend research), 19/09/2026, theo quyết định chủ sản phẩm.** Xoá `facebook-trend-adapter.ts` + test đi kèm, gỡ khỏi `trend-provider-chain.ts` (kênh riêng `facebook` và nhánh Facebook trong Omnichannel), gỡ nhánh `facebook_social` trong `worker.ts`, gỡ mọi lựa chọn/nhãn "Facebook" khỏi UI nghiên cứu (`direct-research-panel.tsx`, `custom-research-modal.tsx`, `saas-admin-criteria-modal.tsx`, `settings-input-tab.tsx`, `feature-settings-modal.tsx`, `use-market-intelligence-data.ts`), và gỡ mục "Thảo luận Facebook" khỏi 2 bộ sinh evidence-reference mặc định (`list-tenant-opportunities.ts`, `personalize-opportunities.ts`) | Facebook không có API công khai đo được mức thảo luận theo chủ đề (Graph API đóng với bên thứ ba, CrowdTangle đã khai tử 8/2024, Meta Content Library chỉ dành học thuật). Adapter cũ chỉ suy diễn qua kết quả tìm kiếm Google (`site:facebook.com`) — không phải số đo Facebook thật — nên chủ sản phẩm quyết định gỡ hẳn thay vì tiếp tục trình bày như một kênh nghiên cứu thật | Chỉ thêm lại nếu có nguồn dữ liệu Facebook thật (vd. tích hợp Facebook Ads Library API cho tín hiệu "mức độ cạnh tranh quảng cáo" — khác chủng loại "mức độ thảo luận", xem `claude/ket-qua-xu-ly-3-van-de-market-intelligence-19-09-2026.md` mục 4) |
| ~~115~~ | **ĐÃ TRẢ 21/09/2026 (chốt bởi Tony: "Phải nối với dữ liệu thật").** `trend-fit.ts` nay tra cứu tín hiệu THẬT từ `trend_signals` (đa nền tảng — Google Trends, SerpApi, TikTok, YouTube, qua `findCachedSignalsAnyPlatform()` mới trong repository) cho cả 5 dòng ma trận đối soát VÀ 10 `evidenceNote`, thay vì hằng số cố định. `matchStatus`/`lifecycle` nay TÍNH từ `growth_rate`/`metric_value` thật (`determineTrendLifecycle()`, trước đây có hàm nhưng không dùng tới) — có thể ra `MISMATCH` thật khi xu hướng giảm, không còn mặc định `MATCH` cho mọi thuộc tính (lỗi phát hiện thêm ngoài phạm vi nợ gốc: `matchStatus` trước đây gán cứng "MATCH" 4/5 dòng bất kể dữ liệu). Khi KHÔNG tìm thấy tín hiệu thật cho một thuộc tính/chủ đề — trả về `PARTIAL` kèm ghi chú trung thực "chưa có dữ liệu thật", không còn bịa số phần trăm. 8/10 mục có số liệu hardcode cũ (top-01,02,03,05,06,07,08 + phần dịp ở top-03) đã đổi sang dùng hàm `honestEvidenceNote()`; 2 mục còn lại (top-04, top-09, top-10) vốn là nhận định marketing định tính chung, không phải số liệu thị trường bịa, giữ nguyên nhưng đã bỏ các con số cụ thể không kiểm chứng được ("28 giây", "gấp 3 lần", "cao nhất trong hệ thống CRM") | Nội dung nợ gốc giữ lại làm hồ sơ: hardcode "+45% lượng tìm kiếm", "250K+ views", "tăng 60%", "8.5 triệu lượt xem", "72% tương tác cao hơn" đều là hằng số cố định, không tính từ `trend_signals`/`provider_health` thật | Cần anh Tony tự chạy `npx prisma generate && npx prisma db push` trên máy thật (phiên này không có mạng để tải engine Prisma) rồi chạy thử `npm run test:tenant` + xem qua UI một sản phẩm thật để xác nhận `matchStatus` ra `MISMATCH` đúng khi có tín hiệu xu hướng giảm thật trong DB |
| 117 | **Ba mảng của kế hoạch Product Intelligence v2.0 tạm hoãn, chưa xây: Occasion Intelligence (mục 14), Positioning (mục 15), Competitive Context (mục 16).** Phát hiện qua rà soát `claude/ra-soat-quet-anh-san-pham-21-09-2026.md`: kế hoạch yêu cầu (a) phân tích riêng theo từng dịp dùng (mức độ liên quan, đối tượng, góc content, chủ đề, bằng chứng — không phải một mảng chuỗi phẳng như hiện tại), (b) gợi ý định vị có giải thích (vd. "Quà sinh nhật"/"Quà lãng mạn"/"Quà tinh tế"), (c) bối cảnh cạnh tranh ("sản phẩm này nằm ở đâu so với thị trường" — phong cách tương tự, đóng gói phổ biến, cơ hội khác biệt hoá). Không có field, hàm, hay component nào cho cả ba trong mã hiện tại (chỉ có cờ boolean `positioningDefined` dùng để tính điểm sẵn sàng, không phải nội dung định vị thật) | **Quyết định chủ sản phẩm 21/09/2026: TẠM HOÃN, chưa xây trong đợt này** — đặc tả 06 mục 22 đã ghi rõ CHƯA XÂY kèm số nợ này để không ai hiểu nhầm route đã trả về đủ 10 mục như UI kế hoạch mục 44 mô tả | Khi Tony chốt làm tiếp — cần bổ sung kiểu dữ liệu mới trong `product-intelligence-types.ts` (vd. `OccasionAnalysis[]`, `PositioningSuggestion[]`, `CompetitiveContext`), domain logic sinh nội dung tương ứng trong `trend-fit.ts`, component UI riêng (nối vào `product-intelligence-workspace.tsx` như 3 mục mới trong luồng 10 mục của mục 44), và cân nhắc có cần bảng `product_positioning` riêng (v2.0 mục 35) hay gộp vào JSONB của `product_analysis_runs` đã có từ nợ #113 |
| 116 | **Kênh nghiên cứu "Google Shopping" (`channel=shopping`) trên UI Market Intelligence là CHƯA XÂY** — `direct-research-panel.tsx` và `custom-research-modal.tsx` cho chọn "🛒 Google Shopping" như một kênh riêng, nhưng `trend-provider-chain.ts` không có nhánh xử lý nào cho `channel === "shopping"` — chọn kênh này hiện âm thầm rơi xuống đúng chuỗi mặc định (Google Trends → SerpApi → Cached), không phải dữ liệu mua sắm/ý định mua thật từ Google Shopping | Chủ sản phẩm quyết định 19/09/2026: **GIỮ** lựa chọn trên UI (không gỡ như Facebook), nhưng phải đánh dấu rõ CHƯA XÂY trong đặc tả 06 mục 22 và chú thích trong mã (`trend-provider-chain.ts`) để không ai hiểu nhầm đây là dữ liệu Google Shopping thật | Khi có adapter Google Shopping thật (vd. qua SerpApi Google Shopping engine) — nối vào `trend-provider-chain.ts` như một nhánh riêng, xoá dòng CHƯA XÂY này |
| 119 | **Khuôn bài đăng Khu vực B còn câu cam kết dịch vụ chung không lấy từ hồ sơ tiệm** — `social-post-generator.ts` ghi "giao nhanh hỏa tốc trong 2 giờ", "tặng thiệp thiết kế", "gói dưỡng hoa 5–7 ngày", "freeship nội thành", "chụp ảnh gửi duyệt". Nếu tiệm không làm các việc này, bài đăng thành cam kết sai với khách. QA Chặng 08 không bắt được (không phải từ cấm) | Rà soát Creative Studio 23/09/2026 chỉ gỡ phần dữ liệu SẢN PHẨM bịa (thành phần, màu, giá 599.000đ); câu dịch vụ là nội dung marketing cần chủ sản phẩm quyết nguồn | Khi thêm các trường dịch vụ vào `business_profiles`/`brand_profiles` (thời gian giao, quà kèm, phí ship) — sinh câu từ đó, thiếu thì bỏ câu |
| 120 | **Nhánh cloud của M04a (`POST /media/optimizations`, engine cloud → `executeCloudCreative`) vẫn chạy đồng bộ trong request HTTP**: không `enqueueJob`, integrity ước lượng 0,98/1,0 thay vì đo, 4 asset `RATIO` trỏ cùng một `storageKey`, lỗi ghi DB bị nuốt (`console.warn`). Cùng loại vi phạm mà nhánh cloud M04b đã được sửa 23/09/2026 | M04a chạy ở `/tai-anh`, ngoài phạm vi đợt Creative Studio 23/09/2026. Đã sửa phần dùng chung: `studio_local` không còn trả ảnh gốc giả nên hết chuỗi nhà cung cấp thì trả LỖI thay vì ghi "ảnh đã tối ưu" giống hệt ảnh gốc | Đưa về cùng khuôn `media.variant.cloud`: job Python, nhà cung cấp chỉ sinh phần được phép sinh, đo Identity Guard thật, dựng 4 tỷ lệ bằng `ratio_frame.dong_khung` |
| 121 | **Năng lực của Creative Studio chưa tách**: Content (B), Audio (C), Video (E), tạo/sửa/QA gói (F) dùng chung `I1` (`media.optimize`); duyệt gói chiến dịch và kế hoạch đăng dùng `J5` (`social.publish`, không trần cứng) | Thêm mã mới đụng con số "143 mã / 39 trần cứng" của catalog và bảng công tắc của mọi tổ chức — quyết định của chủ sản phẩm, không tự đoán (Hiến pháp §2 quy tắc 3) | Khi PO chốt: thêm nhóm mã `creative.*` (vd. run/approve), đặt `hardCap: ["dieu_hanh"]` cho duyệt gói, cập nhật route + đặc tả 06 §23 + Arch §6 cùng commit. **Kèm:** mô hình Stability (`stable-image-core-v2beta`) mà worker `media.variant.cloud` gọi chưa có hàng `ai_models` với bốn ô giấy phép (D18) — phải đăng ký trước go-live |
| 122 | **10 chủ đề Chặng 04 là khuôn theo luật và "video tham khảo" là danh mục tuyển chọn** (`video-evidence-catalog.ts`): metrics YouTube là ước tính, TikTok là trang tìm kiếm với tác giả/lượt tim đặt tay | 23/09/2026 đã nối cờ `isLiveEvidence` ra giao diện: ghi "ước tính", thẻ TikTok thành "Mở kết quả tìm kiếm TikTok", không mượn thumbnail YouTube. Chưa có nguồn video thời gian thực | Khi có API/nguồn thật (SerpApi video, TikTok Research API) — điền `isLiveEvidence: true` từ dữ liệu thật, bỏ danh mục tuyển chọn khỏi đường chính |
| 123 | **[ĐÃ TRẢ 24/09/2026 — Đợt 4: `POST /creative-production/video-assembly` + `video_jobs.audio_storage_key`, worker dùng nguyên bản phối C; chỉ storyboard tay còn tự đọc lại]** **Video Khu vực E chưa dùng bản phối âm thanh của Khu vực C**; giọng và nhạc để worker chọn mặc định | `POST /video/jobs` không có trường nhận audio job/asset; worker render tự sinh âm thanh | Thêm `audio_job_id` vào `video_jobs` + payload render; worker dùng `generation_jobs.output.audio_storage_key` thay vì tự sinh |
| 126 | **`workers/.venv` trên máy dev tạo bằng Python 3.9.6 của Command Line Tools (LibreSSL, không TLS 1.3) trong khi `pyproject.toml` yêu cầu ≥ 3.11** → mọi lượt gọi Stability hỏng `TLSV1_ALERT_PROTOCOL_VERSION`, worker lùi về phông trơn cục bộ nên ảnh Khu vực D trông như ảnh gốc (phát hiện 24/09/2026) | Môi trường máy dev, không phải mã; `npm run worker:*` dùng `.venv/bin/python` nếu có | Tạo lại `.venv` bằng Python ≥ 3.11 rồi `pip install -r requirements.txt`. Từ 24/09 worker in cảnh báo khi khởi động và lý do lỗi hiện rõ trên thẻ cảnh |
| 125 | **Kịch bản bối cảnh theo chủ đề (`creative.scene_plan`, 24/09/2026) sinh ở Chặng 05, nối vào D (bối cảnh ảnh), C (lời thoại), B (hiển thị cung truyện)**: phía máy chủ Khu vực B vẫn dùng `planNarrativeArc` theo luật để sinh bài (bối cảnh chọn theo phong cách, tên preset như `workshop_cozy`/`rooftop` không có ở worker); storyboard Khu vực E đã theo kịch bản (24/09). Giá `creative.scene_plan` = 1 credit là tạm (nợ #64) | PO chốt 24/09: số cảnh theo kịch bản (CREATIVE 5, AUTHENTIC 3), kịch bản do AI viết qua job. Đợt này làm D + C trước vì đó là chỗ lệch thấy được (chủ đề "sinh nhật tone vàng" ra "sảnh khách sạn") | E đã xong 24/09 (storyboard dựng từ kịch bản phía trình duyệt, `video-storyboard-builder.ts`). Còn: cho `produce` (B) đọc `scenePlanId` phía máy chủ, lấy nhịp/lời thoại từ kịch bản thay vì `planNarrativeArc`. Chạy lại `npm run db:seed` để hai mô hình OpenAI có `AIC-18` |
| 127 | **Khi Stability hỏng, lượt biến thể cloud lùi về phông cục bộ nhưng vẫn tính 2 credit (`media.variant.cloud`)** thay vì 1 credit của `media.variant` (phát hiện 24/09/2026) | Credit trừ lúc `enqueueJob`; worker lùi phông trong cùng job, chỉ ghi `cloud_fallback` + `cloud_fallback_reason` | Chờ PO chốt: hoàn phần chênh 1 credit khi `cloud_fallback = true` (worker gọi hoàn một phần) hoặc giữ nguyên và ghi rõ trên giao diện |
| 128 | **4 bài nhạc nền hệ thống (từ P17, `workers/media_ai/video/assets/music/`) không có hồ sơ nguồn/giấy phép** (phát hiện khi rà soát Khu vực C 24/09/2026); catalog trước đó ghi `royalty_free` không căn cứ. Mood "Sang trọng" không có bài nào | Tệp có trong repo từ P17, không kèm nguồn | Đã gắn `licenseVerified: false` + nhãn "Chưa xác minh bản quyền" trên giao diện; tiệm tự tải nhạc có giấy phép được. Còn: PO mua/chọn bộ nhạc royalty-free có giấy phép thương mại, thêm theo quy trình ở đầu `music-catalog.ts`, gỡ 4 bài cũ |
| 129 | **Phạm vi sản xuất (24/09/2026 khuya) — các chỗ chưa theo phạm vi đầy đủ:** (a) `produce` phía máy chủ Khu vực B vẫn viết bài theo luật cho mọi kênh — phạm vi chỉ lọc bài của kịch bản (`content.posts` theo `postChannels`) và QA `scope_coverage`; (b) "Sửa cảnh" ở Chặng 07 sinh ảnh cho MỘT khung và chỉ cập nhật video của khung đang xem — khung khác phải sinh lại ở Khu vực D / dựng lại ở E; (c) bộ chọn phạm vi ghi credit bằng lời ("credit ảnh/video × số khung"), chưa ra số; (d) tab khu vực đọc phạm vi từ URL (`?platforms=&outputs=`), không đọc lại kịch bản nếu phạm vi bị sửa bằng `PATCH` ở phiên khác | PO chốt 24/09 tối: chọn gì sản xuất nấy, mặc định TikTok + Reels 9:16, nhiều khung ⇒ mỗi khung một bộ ảnh + một video, video kéo theo ảnh + âm thanh. Làm phần chặn sai (QA, E, D) trước | (a) cùng nợ #125; (b)(c)(d) khi có phản hồi dùng thật với gói nhiều khung |
| 130 | **Chất lượng ảnh biến thể — việc để Đợt 2–4 (Đợt 1 xong 25/09/2026):** (a) trần phóng bó hoa ×2 (`composition.PHONG_TOI_DA`) — cảnh cận với Master nhỏ sẽ chưa "cận" hẳn cho tới khi có bước tăng nét (Real-ESRGAN có sẵn ở `providers/enhancement/realesrgan.py`); (b) ảnh có tay người cầm: tách nền giữ cả bàn tay, cổ tay bị cụt ở mép (`with_arm_fadeout` có sẵn nhưng tắt); ~~(c) chưa có phương án (`variant_count`) và style~~ — xong Đợt 2 (25/09); (d) chưa đo nhánh Stability bằng `scripts/cham-bien-the.py --cloud` — anh Tony đồng ý đo 12 ảnh 25/09, nhưng tài khoản Stability trả **HTTP 402 (hết credit)**; từ máy của agent `api.stability.ai` còn bị chính sách mạng chặn; ~~(e) `requestVariantBatch` chưa lấy gợi ý từ kịch bản~~ — xong Đợt 2 (`scene_plan_id`/`scene_index` trên `/media/variants/batch`); (f) phương án phông CỤC BỘ khác nhau ít (đo 36 ảnh: độ khác nhau TB 0,036, thấp nhất 0,004 ở cảnh cận / phông trắng) — phông cục bộ chỉ đổi vùng sáng, bố cục bokeh, vị trí bó hoa; khác hẳn hậu cảnh cần nhà cung cấp có seed | Đợt 1 chỉ sửa lỗi nền tảng (dải đệm 31,9% → 0,1% trên 72 ảnh), không đổi giá, không đổi cách đo Subject Integrity | Kế hoạch project claude.ai `claude/ke-hoach-nang-cap-chat-luong-anh-bien-the-24-09-2026.md` — (a)(b) Đợt 3, (d) khi nạp credit Stability, (f) chấp nhận — giao diện nói rõ với chủ tiệm |
| 124 | **`npm run lint` toàn repo đỏ (235 lỗi trong `src/`, 31 trong `tests/`, đều ngoài phạm vi Creative Studio — đo 23/09/2026)** → bước lint của CI (`.github/workflows/ci.yml`) không qua. Phạm vi Creative Studio (page, `components/creative-studio`, `modules/{creative-production,media,audio-studio,video-studio}`, route liên quan) đã về 0 lỗi 23/09/2026 | Đợt Creative Studio không sửa module khác | Dọn theo module; tạm thời CI có thể giới hạn lint theo thư mục đã sạch |
