{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stage-09-approve.input.schema.json",
  "title": "Chặng 09 — APPROVE: Chủ tiệm chốt duyệt — Đầu vào",
  "description": "Chỉ duyệt gói đã chạy QA; QA_REJECTED không duyệt được; QA_NEEDS_REVIEW cần xác nhận đã xem cảnh báo.",
  "x-floraos": {
    "stageId": "09",
    "stage": 9,
    "code": "APPROVE",
    "endpoint": {
      "method": "POST",
      "path": "/api/v1/creative-production/packages/:id/approve",
      "capability": "J5"
    },
    "source": "src/modules/creative-production/contracts/stage-09-approve.ts",
    "direction": "input",
    "generated": "SINH TỰ ĐỘNG — không sửa tay. Sửa zod ở tệp nguồn rồi chạy `npm run gen:schemas:creative`."
  },
  "type": "object",
  "properties": {
    "package_id": {
      "type": "string",
      "minLength": 1,
      "description": "campaign_packages.id — tham số đường dẫn :id"
    },
    "acknowledge_warnings": {
      "default": false,
      "description": "Bắt buộc true khi gói ở QA_NEEDS_REVIEW",
      "type": "boolean"
    }
  },
  "required": [
    "package_id"
  ],
  "examples": [
    {
      "package_id": "c0ffee00-1111-4222-8333-444455556666",
      "acknowledge_warnings": false
    }
  ]
}
