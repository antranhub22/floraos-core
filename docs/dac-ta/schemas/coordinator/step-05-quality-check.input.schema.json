{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "step-05-quality-check.input.schema.json",
  "title": "Kiểm định chất lượng (T14–T15) — Đầu vào",
  "description": "Người kiểm kết luận PASSED / REWORK_REQUESTED / REJECTED; không đạt bắt buộc lý do. Điểm AI chỉ ghi khi có lượt Vision thật (nợ #140)",
  "x-floraos": {
    "stepId": "05",
    "step": 5,
    "code": "QUALITY_CHECK",
    "endpoint": {
      "method": "POST",
      "path": "/api/v1/coordinator/orders/{id}/qc",
      "capability": "R3"
    },
    "source": "src/modules/coordinator/contracts/step-05-quality-check.ts",
    "direction": "input",
    "generated": "SINH TỰ ĐỘNG — không sửa tay. Sửa zod ở tệp nguồn rồi chạy `npm run gen:schemas:coordinator`."
  },
  "type": "object",
  "properties": {
    "decision": {
      "type": "string",
      "enum": [
        "PASSED",
        "REWORK_REQUESTED",
        "REJECTED"
      ]
    },
    "notes": {
      "type": "string",
      "maxLength": 2000
    },
    "checklist": {
      "type": "object",
      "propertyNames": {
        "type": "string",
        "maxLength": 60
      },
      "additionalProperties": {
        "type": "boolean"
      }
    }
  },
  "required": [
    "decision"
  ],
  "examples": [
    {
      "decision": "PASSED",
      "checklist": {
        "flowerMatch": true,
        "colorTone": true,
        "wrapping": true,
        "cardMessage": true
      }
    }
  ]
}
