{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "step-06-delivery.input.schema.json",
  "title": "Giao hàng & POD (T18–T21, F11/F12) — Đầu vào",
  "description": "PICKED_UP → ON_THE_WAY → DELIVERED_SUCCESS (bắt buộc ảnh POD hoặc tên người ký) / DELIVERY_FAILED (mở sự cố)",
  "x-floraos": {
    "stepId": "06",
    "step": 6,
    "code": "DELIVERY",
    "endpoint": {
      "method": "POST",
      "path": "/api/v1/coordinator/orders/{id}/delivery",
      "capability": "R5"
    },
    "source": "src/modules/coordinator/contracts/step-06-delivery.ts",
    "direction": "input",
    "generated": "SINH TỰ ĐỘNG — không sửa tay. Sửa zod ở tệp nguồn rồi chạy `npm run gen:schemas:coordinator`."
  },
  "type": "object",
  "properties": {
    "event": {
      "type": "string",
      "enum": [
        "PICKED_UP",
        "ON_THE_WAY",
        "DELIVERED_SUCCESS",
        "DELIVERY_FAILED"
      ]
    },
    "carrier": {
      "type": "string",
      "maxLength": 100
    },
    "shipperName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "shipperPhone": {
      "type": "string",
      "maxLength": 20
    },
    "podAssetId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64
    },
    "recipientSignedName": {
      "type": "string",
      "maxLength": 200
    },
    "failureReason": {
      "type": "string",
      "maxLength": 1000
    }
  },
  "required": [
    "event",
    "shipperName"
  ],
  "examples": [
    {
      "event": "DELIVERED_SUCCESS",
      "carrier": "AhaMove",
      "shipperName": "Lê Văn Hùng",
      "shipperPhone": "0933221100",
      "recipientSignedName": "Trần Thị Bình"
    }
  ]
}
