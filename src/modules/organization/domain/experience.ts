/**
 * Hạn mức của workspace trải nghiệm (`YC-U6`, đặc tả 09 mục 6).
 *
 * Bốn trường `trial_*` nằm trên `workspaces` và cấu hình được. Việc trừ lượt,
 * khoá thẻ chức năng và nạp dữ liệu mẫu thuộc P10; P1 chỉ đặt giá trị ban đầu
 * để bản ghi không sinh ra với hạn mức bỏ trống.
 */

export const TRIAL_LIMIT = 20

/** Credit ban đầu của một tổ chức trải nghiệm (đặc tả 09 mục 1). */
export const TRIAL_CREDIT_BALANCE = 20

export const DEFAULT_WORKSPACE_NAME = "Workspace chính"
