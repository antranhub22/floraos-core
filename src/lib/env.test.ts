import { describe, expect, it } from "vitest"

/** Test khói: bộ chạy test hoạt động và alias phân giải được. */
describe("nền", () => {
  it("bộ test chạy", () => {
    expect(1 + 1).toBe(2)
  })
})
