{
  "name": "floraos-core",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "test:tenant": "node scripts/check-tenant.mjs"
  }
}
