# Test

| Thư mục | Nội dung |
|---|---|
| `tenant/` | Bộ test cách ly tenant. Bắt buộc xanh trước mọi merge |
| *(theo module)* | Test luật nghiệp vụ thu hoạch, chép kèm mã gốc |

Luật thu hoạch: mã mang từ `FloraOS`, `LocalBudd` hay `SocialFlow` sang phải có hạng trong `HARVEST_MANIFEST.md`, và luật nghiệp vụ phải đi kèm test khoá nó. Test xanh trên repo này thì mới coi là chuyển xong — mã chạy được nhưng chưa có test thì chưa xong.
