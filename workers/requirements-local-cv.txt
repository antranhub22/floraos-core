# Phụ thuộc RIÊNG của bộ máy `local_cv` (Cục bộ) — KHÔNG nằm trong
# `requirements.txt` chính, vì hai bộ Đầy đủ/Gọn không cần torch/transformers
# và không nên bắt một tổ chức chỉ dùng hai bộ đó phải cài vài GB thư viện
# GPU (đúng lý do `registry.py` dựng provider LƯỜI theo từng khoá bộ máy).
#
# Cài trên máy thật có GPU (CUDA hoặc Apple Silicon MPS), KHÔNG cài trong
# VM cô lập của Claude — VM đó không thấy GPU của máy chủ và mạng ra ngoài
# bị chặn tới pypi.org/huggingface.co (xem docstring
# `vision/providers/local_cv_segmenter.py`).
#
# BƯỚC 1 — cài các gói ở tệp này qua pip như bình thường:
#
#   python3.11 -m pip install -r workers/requirements-local-cv.txt
#
# BƯỚC 2 — cài SAM2 RIÊNG, không qua tệp này. Gói `sam2` trên PyPI KHÔNG
# phải do Meta đăng (do bên thứ ba JinsuaFeito-dev đăng, bản cuối 12/2024,
# không đảm bảo khớp cấu trúc module `sam2.build_sam`/`sam2.automatic_mask_generator`
# mà `local_cv_segmenter.py` dùng). Cách chính thức của Meta là clone rồi cài
# editable:
#
#   git clone https://github.com/facebookresearch/sam2.git /tmp/sam2-src
#   cd /tmp/sam2-src && python3.11 -m pip install -e ".[notebooks]"
#
# Yêu cầu Python >= 3.10 theo INSTALL.md chính thức của Meta (repo này khai
# >=3.11 ở pyproject.toml nên đã thoả).
#
# BƯỚC 3 — tải trọng số SAM2 (không có trên PyPI lẫn qua pip cài ở trên,
# đây là tệp checkpoint riêng) theo hướng dẫn trong README chính thức của
# facebookresearch/sam2, rồi điền đường dẫn vào contracts/config.json
# (`sam2_checkpoint`, `sam2_model_config`).
#
# Trọng số Florence-2 tải tự động qua `from_pretrained` khi máy có mạng tới
# huggingface.co — không cần tải tay.

torch>=2.3
torchvision>=0.18
# Ghim đúng 5.15.1 — ĐÃ CHẠY THỬ THẬT 2026-09-11 trên máy thật (MPS).
# 5.17.0 vỡ generate() của Florence-2 (encoder-decoder): 'EncoderDecoderCache'
# object has no attribute 'layers', bên trong chính transformers.generation.utils,
# không phải lỗi ở code của repo này. Chưa rõ 5.17.0 vỡ từ bản nào giữa
# 5.15.1 và 5.17.0 — cứ ghim đúng bản đã xác nhận chạy được, đừng nới lỏng
# thành >= khi chưa thử lại.
transformers==5.15.1
einops>=0.7
timm>=1.0
pytest>=8.0
numpy>=1.26
pillow>=10.2
