# `run_worker` gọi `conn.notifies(timeout=…)` — tham số đó có từ psycopg 3.2.
# Cài đúng 3.1.x thì worker ném TypeError ngay lần thăm dò rỗng đầu tiên.
psycopg[binary]>=3.2
pydantic>=2.6
pillow>=10.2
numpy>=1.26
scipy>=1.12
openai>=1.40
pytest>=8.0
rembg>=2.0
onnxruntime>=1.18
opencv-python>=4.9
# `ReplicateOutpainter` (AIC-13, nợ #78, 17/09) gọi thẳng REST API của
# Replicate — không dùng SDK `replicate` để khỏi thêm một tầng trừu tượng
# chưa cần, cùng cách `openai_enhancer.py` tự quản việc gọi API của mình.
httpx>=0.27
